// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct ProgramCreated {
        pub pool: Vec<u8>,
        pub program_id: substreams::scalar::BigInt,
        pub rewards_token: Vec<u8>,
        pub total_rewards: substreams::scalar::BigInt,
        pub start_time: substreams::scalar::BigInt,
        pub end_time: substreams::scalar::BigInt,
    }
    impl ProgramCreated {
        const TOPIC_ID: [u8; 32] = [
            112u8,
            32u8,
            78u8,
            52u8,
            38u8,
            193u8,
            154u8,
            46u8,
            93u8,
            137u8,
            237u8,
            228u8,
            54u8,
            80u8,
            65u8,
            62u8,
            5u8,
            196u8,
            191u8,
            245u8,
            196u8,
            7u8,
            161u8,
            56u8,
            174u8,
            64u8,
            86u8,
            34u8,
            13u8,
            254u8,
            229u8,
            132u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pool: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool",
                )?,
                program_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "program_id",
                )?,
                rewards_token: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "rewards_token",
                )?,
                total_rewards: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "total_rewards",
                )?,
                start_time: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "start_time",
                )?,
                end_time: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "end_time",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ProgramCreated {
        const NAME: &'static str = "ProgramCreated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ProgramEnabled {
        pub pool: Vec<u8>,
        pub program_id: substreams::scalar::BigInt,
        pub status: bool,
        pub remaining_rewards: substreams::scalar::BigInt,
    }
    impl ProgramEnabled {
        const TOPIC_ID: [u8; 32] = [
            168u8,
            224u8,
            7u8,
            154u8,
            246u8,
            154u8,
            182u8,
            2u8,
            0u8,
            153u8,
            172u8,
            45u8,
            80u8,
            216u8,
            120u8,
            49u8,
            41u8,
            89u8,
            8u8,
            235u8,
            126u8,
            115u8,
            230u8,
            202u8,
            196u8,
            68u8,
            193u8,
            112u8,
            34u8,
            120u8,
            107u8,
            184u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pool: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool",
                )?,
                program_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "program_id",
                )?,
                status: substreams_ethereum::abi::read_bool(log.data(), 0, "status")?,
                remaining_rewards: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "remaining_rewards",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ProgramEnabled {
        const NAME: &'static str = "ProgramEnabled";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ProgramTerminated {
        pub pool: Vec<u8>,
        pub program_id: substreams::scalar::BigInt,
        pub end_time: substreams::scalar::BigInt,
        pub remaining_rewards: substreams::scalar::BigInt,
    }
    impl ProgramTerminated {
        const TOPIC_ID: [u8; 32] = [
            27u8,
            202u8,
            165u8,
            162u8,
            166u8,
            9u8,
            67u8,
            143u8,
            120u8,
            249u8,
            147u8,
            191u8,
            113u8,
            160u8,
            228u8,
            83u8,
            58u8,
            101u8,
            25u8,
            44u8,
            152u8,
            65u8,
            138u8,
            91u8,
            126u8,
            184u8,
            228u8,
            146u8,
            86u8,
            29u8,
            183u8,
            78u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pool: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool",
                )?,
                program_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "program_id",
                )?,
                end_time: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "end_time",
                )?,
                remaining_rewards: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "remaining_rewards",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ProgramTerminated {
        const NAME: &'static str = "ProgramTerminated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}