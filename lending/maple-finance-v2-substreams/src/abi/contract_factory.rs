// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct DefaultVersionSet {
        pub version: substreams::scalar::BigInt,
    }
    impl DefaultVersionSet {
        const TOPIC_ID: [u8; 32] = [
            221u8,
            178u8,
            1u8,
            60u8,
            247u8,
            241u8,
            2u8,
            209u8,
            84u8,
            71u8,
            196u8,
            193u8,
            233u8,
            76u8,
            245u8,
            104u8,
            35u8,
            69u8,
            95u8,
            2u8,
            235u8,
            36u8,
            77u8,
            12u8,
            59u8,
            46u8,
            246u8,
            81u8,
            99u8,
            56u8,
            147u8,
            70u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                version: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "version",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for DefaultVersionSet {
        const NAME: &'static str = "DefaultVersionSet";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ImplementationRegistered {
        pub version: substreams::scalar::BigInt,
        pub implementation_address: Vec<u8>,
        pub initializer: Vec<u8>,
    }
    impl ImplementationRegistered {
        const TOPIC_ID: [u8; 32] = [
            230u8,
            153u8,
            98u8,
            82u8,
            107u8,
            127u8,
            7u8,
            134u8,
            43u8,
            248u8,
            86u8,
            99u8,
            248u8,
            97u8,
            86u8,
            67u8,
            97u8,
            41u8,
            95u8,
            150u8,
            1u8,
            35u8,
            111u8,
            187u8,
            224u8,
            86u8,
            89u8,
            30u8,
            177u8,
            182u8,
            63u8,
            59u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                version: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "version",
                )?,
                implementation_address: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "implementation_address",
                )?,
                initializer: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "initializer",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ImplementationRegistered {
        const NAME: &'static str = "ImplementationRegistered";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct InstanceDeployed {
        pub version: substreams::scalar::BigInt,
        pub instance: Vec<u8>,
        pub initialization_arguments: Vec<u8>,
    }
    impl InstanceDeployed {
        const TOPIC_ID: [u8; 32] = [
            137u8,
            25u8,
            56u8,
            127u8,
            224u8,
            6u8,
            253u8,
            194u8,
            154u8,
            61u8,
            252u8,
            193u8,
            131u8,
            7u8,
            29u8,
            121u8,
            116u8,
            188u8,
            3u8,
            116u8,
            127u8,
            237u8,
            176u8,
            1u8,
            150u8,
            48u8,
            241u8,
            225u8,
            63u8,
            133u8,
            204u8,
            100u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() < 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                version: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "version",
                )?,
                instance: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "instance",
                )?,
                initialization_arguments: substreams_ethereum::abi::read_bytes(
                    log.data(),
                    0,
                    "initialization_arguments",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for InstanceDeployed {
        const NAME: &'static str = "InstanceDeployed";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct InstanceUpgraded {
        pub instance: Vec<u8>,
        pub from_version: substreams::scalar::BigInt,
        pub to_version: substreams::scalar::BigInt,
        pub migration_arguments: Vec<u8>,
    }
    impl InstanceUpgraded {
        const TOPIC_ID: [u8; 32] = [
            251u8,
            180u8,
            243u8,
            107u8,
            112u8,
            219u8,
            168u8,
            236u8,
            237u8,
            200u8,
            179u8,
            131u8,
            97u8,
            244u8,
            79u8,
            27u8,
            12u8,
            97u8,
            224u8,
            78u8,
            196u8,
            224u8,
            204u8,
            246u8,
            32u8,
            100u8,
            157u8,
            197u8,
            88u8,
            87u8,
            63u8,
            95u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() < 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                instance: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "instance",
                )?,
                from_version: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "from_version",
                )?,
                to_version: substreams_ethereum::abi::read_uint(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "to_version",
                )?,
                migration_arguments: substreams_ethereum::abi::read_bytes(
                    log.data(),
                    0,
                    "migration_arguments",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for InstanceUpgraded {
        const NAME: &'static str = "InstanceUpgraded";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct MapleGlobalsSet {
        pub maple_globals: Vec<u8>,
    }
    impl MapleGlobalsSet {
        const TOPIC_ID: [u8; 32] = [
            144u8,
            116u8,
            131u8,
            155u8,
            132u8,
            167u8,
            65u8,
            56u8,
            190u8,
            21u8,
            156u8,
            183u8,
            129u8,
            58u8,
            114u8,
            194u8,
            164u8,
            76u8,
            53u8,
            254u8,
            140u8,
            83u8,
            218u8,
            102u8,
            161u8,
            109u8,
            163u8,
            133u8,
            211u8,
            72u8,
            197u8,
            241u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                maple_globals: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "maple_globals",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for MapleGlobalsSet {
        const NAME: &'static str = "MapleGlobalsSet";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct UpgradePathDisabled {
        pub from_version: substreams::scalar::BigInt,
        pub to_version: substreams::scalar::BigInt,
    }
    impl UpgradePathDisabled {
        const TOPIC_ID: [u8; 32] = [
            164u8,
            111u8,
            26u8,
            221u8,
            194u8,
            35u8,
            107u8,
            125u8,
            147u8,
            237u8,
            42u8,
            138u8,
            80u8,
            127u8,
            28u8,
            71u8,
            204u8,
            146u8,
            101u8,
            109u8,
            43u8,
            111u8,
            130u8,
            191u8,
            8u8,
            118u8,
            218u8,
            158u8,
            150u8,
            75u8,
            158u8,
            94u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                from_version: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from_version",
                )?,
                to_version: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to_version",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for UpgradePathDisabled {
        const NAME: &'static str = "UpgradePathDisabled";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct UpgradePathEnabled {
        pub from_version: substreams::scalar::BigInt,
        pub to_version: substreams::scalar::BigInt,
        pub migrator: Vec<u8>,
    }
    impl UpgradePathEnabled {
        const TOPIC_ID: [u8; 32] = [
            84u8,
            154u8,
            65u8,
            229u8,
            75u8,
            81u8,
            220u8,
            195u8,
            242u8,
            155u8,
            81u8,
            187u8,
            2u8,
            168u8,
            173u8,
            204u8,
            78u8,
            197u8,
            174u8,
            38u8,
            96u8,
            70u8,
            8u8,
            228u8,
            27u8,
            222u8,
            96u8,
            49u8,
            29u8,
            206u8,
            241u8,
            6u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                from_version: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from_version",
                )?,
                to_version: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to_version",
                )?,
                migrator: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "migrator",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for UpgradePathEnabled {
        const NAME: &'static str = "UpgradePathEnabled";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}