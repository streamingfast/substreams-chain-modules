// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct NetworkFeePpmUpdated {
        pub prev_fee_ppm: substreams::scalar::BigInt,
        pub new_fee_ppm: substreams::scalar::BigInt,
    }
    impl NetworkFeePpmUpdated {
        const TOPIC_ID: [u8; 32] = [
            31u8,
            31u8,
            54u8,
            71u8,
            94u8,
            4u8,
            177u8,
            112u8,
            156u8,
            175u8,
            100u8,
            159u8,
            199u8,
            240u8,
            3u8,
            80u8,
            170u8,
            56u8,
            199u8,
            247u8,
            241u8,
            16u8,
            246u8,
            83u8,
            232u8,
            31u8,
            121u8,
            1u8,
            29u8,
            176u8,
            134u8,
            124u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                prev_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "prev_fee_ppm",
                )?,
                new_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_fee_ppm",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NetworkFeePpmUpdated {
        const NAME: &'static str = "NetworkFeePPMUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalFeePpmUpdated {
        pub prev_fee_ppm: substreams::scalar::BigInt,
        pub new_fee_ppm: substreams::scalar::BigInt,
    }
    impl WithdrawalFeePpmUpdated {
        const TOPIC_ID: [u8; 32] = [
            86u8,
            148u8,
            252u8,
            59u8,
            85u8,
            28u8,
            226u8,
            228u8,
            164u8,
            87u8,
            100u8,
            159u8,
            149u8,
            80u8,
            12u8,
            84u8,
            95u8,
            39u8,
            252u8,
            233u8,
            239u8,
            124u8,
            56u8,
            24u8,
            207u8,
            179u8,
            197u8,
            33u8,
            52u8,
            34u8,
            61u8,
            203u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                prev_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "prev_fee_ppm",
                )?,
                new_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_fee_ppm",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalFeePpmUpdated {
        const NAME: &'static str = "WithdrawalFeePPMUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}