// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct EthDistributed {
        pub report_timestamp: substreams::scalar::BigInt,
        pub pre_cl_balance: substreams::scalar::BigInt,
        pub post_cl_balance: substreams::scalar::BigInt,
        pub withdrawals_withdrawn: substreams::scalar::BigInt,
        pub execution_layer_rewards_withdrawn: substreams::scalar::BigInt,
        pub post_buffered_ether: substreams::scalar::BigInt,
    }
    impl EthDistributed {
        const TOPIC_ID: [u8; 32] = [
            146u8,
            221u8,
            60u8,
            177u8,
            73u8,
            161u8,
            238u8,
            189u8,
            81u8,
            253u8,
            140u8,
            42u8,
            54u8,
            83u8,
            253u8,
            150u8,
            243u8,
            12u8,
            74u8,
            192u8,
            29u8,
            79u8,
            133u8,
            15u8,
            193u8,
            109u8,
            70u8,
            171u8,
            214u8,
            195u8,
            233u8,
            47u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 160usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 160usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 160usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                report_timestamp: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "report_timestamp",
                )?,
                pre_cl_balance: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "pre_cl_balance",
                )?,
                post_cl_balance: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "post_cl_balance",
                )?,
                withdrawals_withdrawn: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "withdrawals_withdrawn",
                )?,
                execution_layer_rewards_withdrawn: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "execution_layer_rewards_withdrawn",
                )?,
                post_buffered_ether: substreams_ethereum::abi::read_uint(
                    log.data(),
                    128,
                    "post_buffered_ether",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for EthDistributed {
        const NAME: &'static str = "ETHDistributed";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Submitted {
        pub sender: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub referral: Vec<u8>,
    }
    impl Submitted {
        const TOPIC_ID: [u8; 32] = [
            150u8,
            162u8,
            92u8,
            140u8,
            224u8,
            186u8,
            171u8,
            193u8,
            253u8,
            239u8,
            217u8,
            62u8,
            158u8,
            210u8,
            93u8,
            142u8,
            9u8,
            42u8,
            51u8,
            50u8,
            243u8,
            170u8,
            154u8,
            65u8,
            114u8,
            43u8,
            86u8,
            151u8,
            35u8,
            29u8,
            29u8,
            26u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                sender: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "sender",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                referral: substreams_ethereum::abi::read_address(
                    log.data(),
                    32,
                    "referral",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Submitted {
        const NAME: &'static str = "Submitted";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Transfer {
        pub from: Vec<u8>,
        pub to: Vec<u8>,
        pub value: substreams::scalar::BigInt,
    }
    impl Transfer {
        const TOPIC_ID: [u8; 32] = [
            221u8,
            242u8,
            82u8,
            173u8,
            27u8,
            226u8,
            200u8,
            155u8,
            105u8,
            194u8,
            176u8,
            104u8,
            252u8,
            55u8,
            141u8,
            170u8,
            149u8,
            43u8,
            167u8,
            241u8,
            99u8,
            196u8,
            161u8,
            22u8,
            40u8,
            245u8,
            90u8,
            77u8,
            245u8,
            35u8,
            179u8,
            239u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                from: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from",
                )?,
                to: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to",
                )?,
                value: substreams_ethereum::abi::read_uint(log.data(), 0, "value")?,
            })
        }
    }
    impl substreams_ethereum::Event for Transfer {
        const NAME: &'static str = "Transfer";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}