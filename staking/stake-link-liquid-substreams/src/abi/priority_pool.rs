// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub account: Vec<u8>,
        pub pool_amount: substreams::scalar::BigInt,
        pub queue_amount: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            144u8,
            137u8,
            8u8,
            9u8,
            198u8,
            84u8,
            241u8,
            29u8,
            110u8,
            114u8,
            162u8,
            143u8,
            166u8,
            1u8,
            73u8,
            119u8,
            10u8,
            13u8,
            17u8,
            236u8,
            108u8,
            146u8,
            49u8,
            157u8,
            108u8,
            235u8,
            43u8,
            176u8,
            164u8,
            234u8,
            26u8,
            21u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                account: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "account",
                )?,
                pool_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "pool_amount",
                )?,
                queue_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "queue_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct DepositTokens {
        pub unused_tokens_amount: substreams::scalar::BigInt,
        pub queued_tokens_amount: substreams::scalar::BigInt,
    }
    impl DepositTokens {
        const TOPIC_ID: [u8; 32] = [
            37u8,
            34u8,
            190u8,
            66u8,
            89u8,
            47u8,
            52u8,
            226u8,
            202u8,
            151u8,
            151u8,
            96u8,
            71u8,
            81u8,
            198u8,
            149u8,
            11u8,
            236u8,
            43u8,
            46u8,
            221u8,
            85u8,
            222u8,
            139u8,
            218u8,
            237u8,
            230u8,
            6u8,
            85u8,
            123u8,
            103u8,
            254u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                unused_tokens_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "unused_tokens_amount",
                )?,
                queued_tokens_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "queued_tokens_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for DepositTokens {
        const NAME: &'static str = "DepositTokens";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct UnqueueTokens {
        pub account: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl UnqueueTokens {
        const TOPIC_ID: [u8; 32] = [
            137u8,
            112u8,
            34u8,
            26u8,
            111u8,
            170u8,
            29u8,
            53u8,
            158u8,
            27u8,
            146u8,
            7u8,
            189u8,
            247u8,
            17u8,
            189u8,
            99u8,
            19u8,
            39u8,
            55u8,
            75u8,
            118u8,
            73u8,
            129u8,
            196u8,
            103u8,
            249u8,
            114u8,
            124u8,
            148u8,
            161u8,
            245u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                account: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "account",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for UnqueueTokens {
        const NAME: &'static str = "UnqueueTokens";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdraw {
        pub account: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl Withdraw {
        const TOPIC_ID: [u8; 32] = [
            136u8,
            78u8,
            218u8,
            217u8,
            206u8,
            111u8,
            162u8,
            68u8,
            13u8,
            138u8,
            84u8,
            204u8,
            18u8,
            52u8,
            144u8,
            235u8,
            150u8,
            210u8,
            118u8,
            132u8,
            121u8,
            212u8,
            159u8,
            249u8,
            199u8,
            54u8,
            97u8,
            37u8,
            169u8,
            66u8,
            67u8,
            100u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                account: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "account",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for Withdraw {
        const NAME: &'static str = "Withdraw";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}