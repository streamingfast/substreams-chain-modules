// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AssetAllocated {
        pub asset: Vec<u8>,
        pub strategy: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl AssetAllocated {
        const TOPIC_ID: [u8; 32] = [
            65u8,
            185u8,
            150u8,
            89u8,
            246u8,
            186u8,
            8u8,
            3u8,
            244u8,
            68u8,
            175u8,
            242u8,
            158u8,
            91u8,
            246u8,
            226u8,
            109u8,
            216u8,
            106u8,
            50u8,
            25u8,
            175u8,
            249u8,
            33u8,
            25u8,
            214u8,
            151u8,
            16u8,
            169u8,
            86u8,
            186u8,
            141u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                asset: substreams_ethereum::abi::read_address(log.data(), 0, "asset")?,
                strategy: substreams_ethereum::abi::read_address(
                    log.data(),
                    32,
                    "strategy",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 64, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for AssetAllocated {
        const NAME: &'static str = "AssetAllocated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Redeem {
        pub addr: Vec<u8>,
        pub value: substreams::scalar::BigInt,
    }
    impl Redeem {
        const TOPIC_ID: [u8; 32] = [
            34u8,
            40u8,
            56u8,
            219u8,
            39u8,
            148u8,
            209u8,
            21u8,
            50u8,
            217u8,
            64u8,
            232u8,
            222u8,
            195u8,
            138u8,
            227u8,
            7u8,
            237u8,
            11u8,
            99u8,
            205u8,
            151u8,
            194u8,
            51u8,
            50u8,
            46u8,
            34u8,
            31u8,
            153u8,
            135u8,
            103u8,
            166u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                addr: substreams_ethereum::abi::read_address(log.data(), 0, "addr")?,
                value: substreams_ethereum::abi::read_uint(log.data(), 32, "value")?,
            })
        }
    }
    impl substreams_ethereum::Event for Redeem {
        const NAME: &'static str = "Redeem";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalClaimed {
        pub withdrawer: Vec<u8>,
        pub request_id: substreams::scalar::BigInt,
        pub amount: substreams::scalar::BigInt,
    }
    impl WithdrawalClaimed {
        const TOPIC_ID: [u8; 32] = [
            45u8,
            67u8,
            235u8,
            23u8,
            71u8,
            135u8,
            21u8,
            81u8,
            50u8,
            181u8,
            45u8,
            219u8,
            107u8,
            52u8,
            110u8,
            45u8,
            202u8,
            153u8,
            48u8,
            46u8,
            172u8,
            61u8,
            244u8,
            70u8,
            109u8,
            190u8,
            255u8,
            149u8,
            61u8,
            60u8,
            132u8,
            209u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                withdrawer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "withdrawer",
                )?,
                request_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "request_id",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalClaimed {
        const NAME: &'static str = "WithdrawalClaimed";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalRequested {
        pub withdrawer: Vec<u8>,
        pub request_id: substreams::scalar::BigInt,
        pub amount: substreams::scalar::BigInt,
        pub queued: substreams::scalar::BigInt,
    }
    impl WithdrawalRequested {
        const TOPIC_ID: [u8; 32] = [
            56u8,
            227u8,
            217u8,
            114u8,
            148u8,
            124u8,
            254u8,
            249u8,
            66u8,
            5u8,
            22u8,
            61u8,
            72u8,
            61u8,
            98u8,
            135u8,
            239u8,
            39u8,
            235u8,
            49u8,
            46u8,
            32u8,
            203u8,
            142u8,
            11u8,
            19u8,
            164u8,
            153u8,
            137u8,
            219u8,
            35u8,
            46u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                withdrawer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "withdrawer",
                )?,
                request_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "request_id",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                queued: substreams_ethereum::abi::read_uint(log.data(), 32, "queued")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalRequested {
        const NAME: &'static str = "WithdrawalRequested";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct YieldDistribution {
        pub to: Vec<u8>,
        pub yield_amount: substreams::scalar::BigInt,
        pub fee: substreams::scalar::BigInt,
    }
    impl YieldDistribution {
        const TOPIC_ID: [u8; 32] = [
            9u8,
            81u8,
            110u8,
            207u8,
            74u8,
            138u8,
            134u8,
            229u8,
            151u8,
            128u8,
            169u8,
            190u8,
            252u8,
            109u8,
            238u8,
            148u8,
            139u8,
            201u8,
            230u8,
            10u8,
            54u8,
            227u8,
            190u8,
            104u8,
            211u8,
            30u8,
            168u8,
            23u8,
            238u8,
            141u8,
            44u8,
            128u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                to: substreams_ethereum::abi::read_address(log.data(), 0, "to")?,
                yield_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "yield_amount",
                )?,
                fee: substreams_ethereum::abi::read_uint(log.data(), 64, "fee")?,
            })
        }
    }
    impl substreams_ethereum::Event for YieldDistribution {
        const NAME: &'static str = "YieldDistribution";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}