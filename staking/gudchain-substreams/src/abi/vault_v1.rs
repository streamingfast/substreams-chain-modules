// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub token: Vec<u8>,
        pub user: Vec<u8>,
        pub deposit_amt: substreams::scalar::BigInt,
        pub share_issued: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            220u8,
            188u8,
            28u8,
            5u8,
            36u8,
            15u8,
            49u8,
            255u8,
            58u8,
            208u8,
            103u8,
            239u8,
            30u8,
            227u8,
            92u8,
            228u8,
            153u8,
            119u8,
            98u8,
            117u8,
            46u8,
            58u8,
            9u8,
            82u8,
            132u8,
            117u8,
            69u8,
            68u8,
            244u8,
            199u8,
            9u8,
            215u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                user: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                deposit_amt: substreams_ethereum::abi::read_uint(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "deposit_amt",
                )?,
                share_issued: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "share_issued",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdraw {
        pub token: Vec<u8>,
        pub user: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub share_burnt: substreams::scalar::BigInt,
    }
    impl Withdraw {
        const TOPIC_ID: [u8; 32] = [
            243u8,
            65u8,
            36u8,
            106u8,
            218u8,
            172u8,
            111u8,
            73u8,
            123u8,
            194u8,
            166u8,
            86u8,
            245u8,
            70u8,
            171u8,
            158u8,
            24u8,
            33u8,
            17u8,
            214u8,
            48u8,
            57u8,
            79u8,
            12u8,
            87u8,
            199u8,
            16u8,
            165u8,
            154u8,
            44u8,
            181u8,
            103u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                user: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                amount: substreams_ethereum::abi::read_uint(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "amount",
                )?,
                share_burnt: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "share_burnt",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Withdraw {
        const NAME: &'static str = "Withdraw";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}