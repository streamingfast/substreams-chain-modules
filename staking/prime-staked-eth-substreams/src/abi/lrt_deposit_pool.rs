// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AssetDeposit {
        pub depositor: Vec<u8>,
        pub asset: Vec<u8>,
        pub deposit_amount: substreams::scalar::BigInt,
        pub prime_eth_mint_amount: substreams::scalar::BigInt,
        pub referral_id: String,
    }
    impl AssetDeposit {
        const TOPIC_ID: [u8; 32] = [
            7u8,
            195u8,
            31u8,
            204u8,
            245u8,
            25u8,
            150u8,
            240u8,
            244u8,
            234u8,
            1u8,
            195u8,
            165u8,
            81u8,
            145u8,
            120u8,
            107u8,
            58u8,
            140u8,
            216u8,
            159u8,
            105u8,
            109u8,
            180u8,
            212u8,
            42u8,
            218u8,
            169u8,
            155u8,
            14u8,
            21u8,
            241u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() < 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                depositor: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "depositor",
                )?,
                asset: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "asset",
                )?,
                deposit_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "deposit_amount",
                )?,
                prime_eth_mint_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "prime_eth_mint_amount",
                )?,
                referral_id: substreams_ethereum::abi::read_string(
                    log.data(),
                    64,
                    "referral_id",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for AssetDeposit {
        const NAME: &'static str = "AssetDeposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct AssetSwapped {
        pub from_asset: Vec<u8>,
        pub to_asset: Vec<u8>,
        pub from_asset_amount: substreams::scalar::BigInt,
        pub to_asset_amount: substreams::scalar::BigInt,
    }
    impl AssetSwapped {
        const TOPIC_ID: [u8; 32] = [
            74u8,
            197u8,
            223u8,
            64u8,
            217u8,
            16u8,
            254u8,
            171u8,
            116u8,
            240u8,
            44u8,
            68u8,
            48u8,
            86u8,
            143u8,
            153u8,
            231u8,
            17u8,
            37u8,
            121u8,
            6u8,
            221u8,
            13u8,
            241u8,
            22u8,
            67u8,
            223u8,
            34u8,
            242u8,
            238u8,
            60u8,
            246u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                from_asset: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from_asset",
                )?,
                to_asset: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to_asset",
                )?,
                from_asset_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "from_asset_amount",
                )?,
                to_asset_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "to_asset_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for AssetSwapped {
        const NAME: &'static str = "AssetSwapped";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalClaimed {
        pub withdrawer: Vec<u8>,
        pub asset: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
    }
    impl WithdrawalClaimed {
        const TOPIC_ID: [u8; 32] = [
            129u8,
            136u8,
            226u8,
            180u8,
            217u8,
            95u8,
            115u8,
            219u8,
            48u8,
            105u8,
            11u8,
            65u8,
            3u8,
            199u8,
            17u8,
            89u8,
            52u8,
            155u8,
            184u8,
            151u8,
            223u8,
            146u8,
            137u8,
            2u8,
            198u8,
            51u8,
            14u8,
            249u8,
            158u8,
            69u8,
            254u8,
            243u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                withdrawer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "withdrawer",
                )?,
                asset: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "asset",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalClaimed {
        const NAME: &'static str = "WithdrawalClaimed";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}