// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Invested {
        pub amount: substreams::scalar::BigInt,
        pub post_buffered_assets: substreams::scalar::BigInt,
        pub post_invested_assets: substreams::scalar::BigInt,
    }
    impl Invested {
        const TOPIC_ID: [u8; 32] = [
            21u8,
            41u8,
            74u8,
            217u8,
            212u8,
            46u8,
            43u8,
            189u8,
            68u8,
            109u8,
            79u8,
            246u8,
            202u8,
            40u8,
            254u8,
            248u8,
            7u8,
            209u8,
            99u8,
            26u8,
            213u8,
            60u8,
            104u8,
            131u8,
            3u8,
            254u8,
            70u8,
            132u8,
            16u8,
            131u8,
            15u8,
            50u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                post_buffered_assets: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "post_buffered_assets",
                )?,
                post_invested_assets: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "post_invested_assets",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Invested {
        const NAME: &'static str = "Invested";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SharesBurnt {
        pub account: Vec<u8>,
        pub pre_rebase_token_amount: substreams::scalar::BigInt,
        pub post_rebase_token_amount: substreams::scalar::BigInt,
        pub shares_amount: substreams::scalar::BigInt,
    }
    impl SharesBurnt {
        const TOPIC_ID: [u8; 32] = [
            139u8,
            42u8,
            30u8,
            26u8,
            213u8,
            224u8,
            87u8,
            140u8,
            61u8,
            216u8,
            36u8,
            148u8,
            21u8,
            110u8,
            152u8,
            93u8,
            173u8,
            232u8,
            39u8,
            168u8,
            124u8,
            87u8,
            59u8,
            92u8,
            28u8,
            119u8,
            22u8,
            163u8,
            33u8,
            98u8,
            173u8,
            100u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                account: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "account",
                )?,
                pre_rebase_token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "pre_rebase_token_amount",
                )?,
                post_rebase_token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "post_rebase_token_amount",
                )?,
                shares_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "shares_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for SharesBurnt {
        const NAME: &'static str = "SharesBurnt";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Submitted {
        pub sender: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub referral: Vec<u8>,
    }
    impl Submitted {
        const TOPIC_ID: [u8; 32] = [
            150u8,
            162u8,
            92u8,
            140u8,
            224u8,
            186u8,
            171u8,
            193u8,
            253u8,
            239u8,
            217u8,
            62u8,
            158u8,
            210u8,
            93u8,
            142u8,
            9u8,
            42u8,
            51u8,
            50u8,
            243u8,
            170u8,
            154u8,
            65u8,
            114u8,
            43u8,
            86u8,
            151u8,
            35u8,
            29u8,
            29u8,
            26u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                sender: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "sender",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                referral: substreams_ethereum::abi::read_address(
                    log.data(),
                    32,
                    "referral",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Submitted {
        const NAME: &'static str = "Submitted";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}