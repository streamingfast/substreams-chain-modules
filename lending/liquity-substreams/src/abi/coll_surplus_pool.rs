// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct ActivePoolAddressChanged {
        pub new_active_pool_address: Vec<u8>,
    }
    impl ActivePoolAddressChanged {
        const TOPIC_ID: [u8; 32] = [
            120u8,
            240u8,
            88u8,
            177u8,
            137u8,
            23u8,
            84u8,
            48u8,
            196u8,
            141u8,
            192u8,
            38u8,
            153u8,
            227u8,
            160u8,
            3u8,
            30u8,
            164u8,
            255u8,
            120u8,
            21u8,
            54u8,
            220u8,
            47u8,
            171u8,
            132u8,
            125u8,
            228u8,
            186u8,
            189u8,
            216u8,
            130u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                new_active_pool_address: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "new_active_pool_address",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ActivePoolAddressChanged {
        const NAME: &'static str = "ActivePoolAddressChanged";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct BorrowerOperationsAddressChanged {
        pub new_borrower_operations_address: Vec<u8>,
    }
    impl BorrowerOperationsAddressChanged {
        const TOPIC_ID: [u8; 32] = [
            60u8,
            166u8,
            49u8,
            255u8,
            205u8,
            42u8,
            155u8,
            93u8,
            154u8,
            225u8,
            133u8,
            67u8,
            252u8,
            130u8,
            245u8,
            142u8,
            180u8,
            202u8,
            51u8,
            175u8,
            158u8,
            106u8,
            176u8,
            27u8,
            122u8,
            142u8,
            149u8,
            51u8,
            30u8,
            110u8,
            217u8,
            133u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                new_borrower_operations_address: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "new_borrower_operations_address",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for BorrowerOperationsAddressChanged {
        const NAME: &'static str = "BorrowerOperationsAddressChanged";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct CollBalanceUpdated {
        pub account: Vec<u8>,
        pub new_balance: substreams::scalar::BigInt,
    }
    impl CollBalanceUpdated {
        const TOPIC_ID: [u8; 32] = [
            240u8,
            57u8,
            58u8,
            52u8,
            208u8,
            94u8,
            101u8,
            103u8,
            104u8,
            106u8,
            212u8,
            224u8,
            151u8,
            249u8,
            217u8,
            210u8,
            120u8,
            21u8,
            101u8,
            149u8,
            115u8,
            148u8,
            241u8,
            240u8,
            217u8,
            132u8,
            229u8,
            216u8,
            230u8,
            55u8,
            143u8,
            32u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                account: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "account",
                )?,
                new_balance: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "new_balance",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for CollBalanceUpdated {
        const NAME: &'static str = "CollBalanceUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EtherSent {
        pub to: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl EtherSent {
        const TOPIC_ID: [u8; 32] = [
            97u8,
            9u8,
            226u8,
            85u8,
            157u8,
            250u8,
            118u8,
            106u8,
            174u8,
            199u8,
            17u8,
            131u8,
            81u8,
            212u8,
            138u8,
            82u8,
            63u8,
            10u8,
            65u8,
            87u8,
            244u8,
            156u8,
            141u8,
            104u8,
            116u8,
            156u8,
            138u8,
            196u8,
            19u8,
            24u8,
            173u8,
            18u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                to: substreams_ethereum::abi::read_address(log.data(), 0, "to")?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 32, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for EtherSent {
        const NAME: &'static str = "EtherSent";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OwnershipTransferred {
        pub previous_owner: Vec<u8>,
        pub new_owner: Vec<u8>,
    }
    impl OwnershipTransferred {
        const TOPIC_ID: [u8; 32] = [
            139u8,
            224u8,
            7u8,
            156u8,
            83u8,
            22u8,
            89u8,
            20u8,
            19u8,
            68u8,
            205u8,
            31u8,
            208u8,
            164u8,
            242u8,
            132u8,
            25u8,
            73u8,
            127u8,
            151u8,
            34u8,
            163u8,
            218u8,
            175u8,
            227u8,
            180u8,
            24u8,
            111u8,
            107u8,
            100u8,
            87u8,
            224u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                previous_owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "previous_owner",
                )?,
                new_owner: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "new_owner",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OwnershipTransferred {
        const NAME: &'static str = "OwnershipTransferred";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TroveManagerAddressChanged {
        pub new_trove_manager_address: Vec<u8>,
    }
    impl TroveManagerAddressChanged {
        const TOPIC_ID: [u8; 32] = [
            20u8,
            50u8,
            25u8,
            201u8,
            230u8,
            155u8,
            9u8,
            224u8,
            126u8,
            9u8,
            95u8,
            204u8,
            136u8,
            155u8,
            67u8,
            216u8,
            244u8,
            108u8,
            168u8,
            146u8,
            187u8,
            166u8,
            95u8,
            8u8,
            220u8,
            58u8,
            0u8,
            80u8,
            134u8,
            154u8,
            86u8,
            120u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                new_trove_manager_address: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "new_trove_manager_address",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TroveManagerAddressChanged {
        const NAME: &'static str = "TroveManagerAddressChanged";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}