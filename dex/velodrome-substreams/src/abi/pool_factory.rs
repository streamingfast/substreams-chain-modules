// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct PoolCreated {
        pub token0: Vec<u8>,
        pub token1: Vec<u8>,
        pub stable: bool,
        pub pool: Vec<u8>,
        pub all_pools_length: substreams::scalar::BigInt,
    }
    impl PoolCreated {
        const TOPIC_ID: [u8; 32] = [
            33u8,
            40u8,
            216u8,
            141u8,
            20u8,
            200u8,
            12u8,
            176u8,
            129u8,
            193u8,
            37u8,
            42u8,
            90u8,
            207u8,
            247u8,
            162u8,
            100u8,
            103u8,
            27u8,
            241u8,
            153u8,
            206u8,
            34u8,
            107u8,
            83u8,
            120u8,
            143u8,
            178u8,
            96u8,
            101u8,
            0u8,
            94u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token0: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token0",
                )?,
                token1: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "token1",
                )?,
                stable: substreams_ethereum::abi::read_bool(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "stable",
                )?,
                pool: substreams_ethereum::abi::read_address(log.data(), 0, "pool")?,
                all_pools_length: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "all_pools_length",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for PoolCreated {
        const NAME: &'static str = "PoolCreated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}