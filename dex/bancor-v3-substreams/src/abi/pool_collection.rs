// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct DefaultTradingFeePpmUpdated {
        pub prev_fee_ppm: substreams::scalar::BigInt,
        pub new_fee_ppm: substreams::scalar::BigInt,
    }
    impl DefaultTradingFeePpmUpdated {
        const TOPIC_ID: [u8; 32] = [
            184u8,
            4u8,
            176u8,
            236u8,
            63u8,
            14u8,
            164u8,
            48u8,
            4u8,
            135u8,
            13u8,
            243u8,
            119u8,
            23u8,
            12u8,
            228u8,
            76u8,
            183u8,
            137u8,
            173u8,
            202u8,
            22u8,
            210u8,
            50u8,
            134u8,
            227u8,
            55u8,
            96u8,
            85u8,
            88u8,
            17u8,
            127u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                prev_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "prev_fee_ppm",
                )?,
                new_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_fee_ppm",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for DefaultTradingFeePpmUpdated {
        const NAME: &'static str = "DefaultTradingFeePPMUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TokensDeposited {
        pub context_id: [u8; 32usize],
        pub provider: Vec<u8>,
        pub token: Vec<u8>,
        pub token_amount: substreams::scalar::BigInt,
        pub pool_token_amount: substreams::scalar::BigInt,
    }
    impl TokensDeposited {
        const TOPIC_ID: [u8; 32] = [
            236u8,
            183u8,
            228u8,
            205u8,
            21u8,
            128u8,
            71u8,
            42u8,
            218u8,
            235u8,
            167u8,
            18u8,
            179u8,
            106u8,
            207u8,
            148u8,
            67u8,
            155u8,
            46u8,
            23u8,
            96u8,
            175u8,
            85u8,
            254u8,
            219u8,
            97u8,
            150u8,
            12u8,
            164u8,
            66u8,
            42u8,
            243u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                context_id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "context_id")?,
                provider: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "provider",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "token_amount",
                )?,
                pool_token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "pool_token_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TokensDeposited {
        const NAME: &'static str = "TokensDeposited";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TokensWithdrawn {
        pub context_id: [u8; 32usize],
        pub provider: Vec<u8>,
        pub token: Vec<u8>,
        pub token_amount: substreams::scalar::BigInt,
        pub pool_token_amount: substreams::scalar::BigInt,
        pub external_protection_base_token_amount: substreams::scalar::BigInt,
        pub bnt_amount: substreams::scalar::BigInt,
        pub withdrawal_fee_amount: substreams::scalar::BigInt,
    }
    impl TokensWithdrawn {
        const TOPIC_ID: [u8; 32] = [
            234u8,
            184u8,
            172u8,
            158u8,
            148u8,
            120u8,
            164u8,
            179u8,
            195u8,
            122u8,
            121u8,
            78u8,
            206u8,
            246u8,
            41u8,
            184u8,
            168u8,
            187u8,
            205u8,
            150u8,
            249u8,
            234u8,
            234u8,
            200u8,
            237u8,
            38u8,
            5u8,
            77u8,
            20u8,
            77u8,
            165u8,
            45u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 160usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 160usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 160usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                context_id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "context_id")?,
                provider: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "provider",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "token_amount",
                )?,
                pool_token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "pool_token_amount",
                )?,
                external_protection_base_token_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "external_protection_base_token_amount",
                )?,
                bnt_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "bnt_amount",
                )?,
                withdrawal_fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    128,
                    "withdrawal_fee_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TokensWithdrawn {
        const NAME: &'static str = "TokensWithdrawn";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TotalLiquidityUpdated {
        pub context_id: [u8; 32usize],
        pub pool: Vec<u8>,
        pub liquidity: substreams::scalar::BigInt,
        pub staked_balance: substreams::scalar::BigInt,
        pub pool_token_supply: substreams::scalar::BigInt,
    }
    impl TotalLiquidityUpdated {
        const TOPIC_ID: [u8; 32] = [
            103u8,
            196u8,
            134u8,
            63u8,
            23u8,
            218u8,
            152u8,
            71u8,
            197u8,
            124u8,
            159u8,
            160u8,
            67u8,
            230u8,
            58u8,
            13u8,
            223u8,
            79u8,
            90u8,
            134u8,
            253u8,
            99u8,
            166u8,
            247u8,
            30u8,
            166u8,
            144u8,
            87u8,
            161u8,
            28u8,
            65u8,
            26u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                context_id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "context_id")?,
                pool: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "pool",
                )?,
                liquidity: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "liquidity",
                )?,
                staked_balance: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "staked_balance",
                )?,
                pool_token_supply: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "pool_token_supply",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TotalLiquidityUpdated {
        const NAME: &'static str = "TotalLiquidityUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TradingFeePpmUpdated {
        pub pool: Vec<u8>,
        pub prev_fee_ppm: substreams::scalar::BigInt,
        pub new_fee_ppm: substreams::scalar::BigInt,
    }
    impl TradingFeePpmUpdated {
        const TOPIC_ID: [u8; 32] = [
            162u8,
            214u8,
            108u8,
            30u8,
            44u8,
            196u8,
            178u8,
            204u8,
            93u8,
            170u8,
            171u8,
            192u8,
            100u8,
            71u8,
            84u8,
            46u8,
            152u8,
            158u8,
            215u8,
            72u8,
            169u8,
            251u8,
            34u8,
            71u8,
            107u8,
            108u8,
            145u8,
            61u8,
            204u8,
            28u8,
            127u8,
            75u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pool: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool",
                )?,
                prev_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "prev_fee_ppm",
                )?,
                new_fee_ppm: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_fee_ppm",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TradingFeePpmUpdated {
        const NAME: &'static str = "TradingFeePPMUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}