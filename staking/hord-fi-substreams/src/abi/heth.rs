// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct StakingStatsUpdated {
        pub new_rewards_amount: substreams::scalar::BigInt,
        pub new_total_eth_balance_in_validators: substreams::scalar::BigInt,
        pub new_total_execution_layer_rewards: substreams::scalar::BigInt,
    }
    impl StakingStatsUpdated {
        const TOPIC_ID: [u8; 32] = [
            44u8,
            202u8,
            91u8,
            170u8,
            74u8,
            25u8,
            191u8,
            116u8,
            165u8,
            29u8,
            72u8,
            168u8,
            228u8,
            78u8,
            155u8,
            76u8,
            2u8,
            50u8,
            15u8,
            107u8,
            9u8,
            205u8,
            164u8,
            247u8,
            194u8,
            2u8,
            194u8,
            127u8,
            116u8,
            142u8,
            230u8,
            150u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                new_rewards_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "new_rewards_amount",
                )?,
                new_total_eth_balance_in_validators: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_total_eth_balance_in_validators",
                )?,
                new_total_execution_layer_rewards: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "new_total_execution_layer_rewards",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for StakingStatsUpdated {
        const NAME: &'static str = "StakingStatsUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Transfer {
        pub from: Vec<u8>,
        pub to: Vec<u8>,
        pub value: substreams::scalar::BigInt,
    }
    impl Transfer {
        const TOPIC_ID: [u8; 32] = [
            221u8,
            242u8,
            82u8,
            173u8,
            27u8,
            226u8,
            200u8,
            155u8,
            105u8,
            194u8,
            176u8,
            104u8,
            252u8,
            55u8,
            141u8,
            170u8,
            149u8,
            43u8,
            167u8,
            241u8,
            99u8,
            196u8,
            161u8,
            22u8,
            40u8,
            245u8,
            90u8,
            77u8,
            245u8,
            35u8,
            179u8,
            239u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                from: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from",
                )?,
                to: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to",
                )?,
                value: substreams_ethereum::abi::read_uint(log.data(), 0, "value")?,
            })
        }
    }
    impl substreams_ethereum::Event for Transfer {
        const NAME: &'static str = "Transfer";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}