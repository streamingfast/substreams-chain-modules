// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct TransferBatch {
        pub operator: Vec<u8>,
        pub from: Vec<u8>,
        pub to: Vec<u8>,
        pub ids: Vec<substreams::scalar::BigInt>,
        pub values: Vec<substreams::scalar::BigInt>,
    }
    impl TransferBatch {
        const TOPIC_ID: [u8; 32] = [
            74u8,
            57u8,
            220u8,
            6u8,
            212u8,
            192u8,
            219u8,
            198u8,
            75u8,
            112u8,
            175u8,
            144u8,
            253u8,
            105u8,
            138u8,
            35u8,
            58u8,
            81u8,
            138u8,
            165u8,
            208u8,
            126u8,
            89u8,
            93u8,
            152u8,
            59u8,
            140u8,
            5u8,
            38u8,
            200u8,
            247u8,
            251u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() < 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                operator: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "operator",
                )?,
                from: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "from",
                )?,
                to: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "to",
                )?,
                ids: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        0,
                        "ids",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(substreams_ethereum::abi::read_uint(tail, at, "ids")?);
                        at += 32usize;
                    }
                    out
                },
                values: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        32,
                        "values",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(
                            substreams_ethereum::abi::read_uint(tail, at, "values")?,
                        );
                        at += 32usize;
                    }
                    out
                },
            })
        }
    }
    impl substreams_ethereum::Event for TransferBatch {
        const NAME: &'static str = "TransferBatch";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TransferSingle {
        pub operator: Vec<u8>,
        pub from: Vec<u8>,
        pub to: Vec<u8>,
        pub id: substreams::scalar::BigInt,
        pub value: substreams::scalar::BigInt,
    }
    impl TransferSingle {
        const TOPIC_ID: [u8; 32] = [
            195u8,
            213u8,
            129u8,
            104u8,
            197u8,
            174u8,
            115u8,
            151u8,
            115u8,
            29u8,
            6u8,
            61u8,
            91u8,
            191u8,
            61u8,
            101u8,
            120u8,
            84u8,
            66u8,
            115u8,
            67u8,
            244u8,
            192u8,
            131u8,
            36u8,
            15u8,
            122u8,
            172u8,
            170u8,
            45u8,
            15u8,
            98u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                operator: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "operator",
                )?,
                from: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "from",
                )?,
                to: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "to",
                )?,
                id: substreams_ethereum::abi::read_uint(log.data(), 0, "id")?,
                value: substreams_ethereum::abi::read_uint(log.data(), 32, "value")?,
            })
        }
    }
    impl substreams_ethereum::Event for TransferSingle {
        const NAME: &'static str = "TransferSingle";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}