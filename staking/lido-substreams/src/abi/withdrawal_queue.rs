// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalClaimed {
        pub request_id: substreams::scalar::BigInt,
        pub owner: Vec<u8>,
        pub receiver: Vec<u8>,
        pub amount_of_eth: substreams::scalar::BigInt,
    }
    impl WithdrawalClaimed {
        const TOPIC_ID: [u8; 32] = [
            106u8,
            210u8,
            108u8,
            94u8,
            35u8,
            142u8,
            125u8,
            0u8,
            39u8,
            153u8,
            249u8,
            165u8,
            219u8,
            7u8,
            232u8,
            30u8,
            241u8,
            78u8,
            55u8,
            56u8,
            106u8,
            224u8,
            52u8,
            150u8,
            215u8,
            167u8,
            239u8,
            4u8,
            113u8,
            62u8,
            20u8,
            91u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                request_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "request_id",
                )?,
                owner: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                amount_of_eth: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "amount_of_eth",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalClaimed {
        const NAME: &'static str = "WithdrawalClaimed";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawalRequested {
        pub request_id: substreams::scalar::BigInt,
        pub requestor: Vec<u8>,
        pub owner: Vec<u8>,
        pub amount_of_st_eth: substreams::scalar::BigInt,
        pub amount_of_shares: substreams::scalar::BigInt,
    }
    impl WithdrawalRequested {
        const TOPIC_ID: [u8; 32] = [
            240u8,
            203u8,
            71u8,
            31u8,
            35u8,
            251u8,
            116u8,
            234u8,
            68u8,
            184u8,
            37u8,
            46u8,
            177u8,
            136u8,
            26u8,
            45u8,
            202u8,
            84u8,
            98u8,
            136u8,
            217u8,
            246u8,
            233u8,
            13u8,
            26u8,
            14u8,
            130u8,
            254u8,
            14u8,
            211u8,
            66u8,
            171u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                request_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "request_id",
                )?,
                requestor: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "requestor",
                )?,
                owner: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                amount_of_st_eth: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "amount_of_st_eth",
                )?,
                amount_of_shares: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "amount_of_shares",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawalRequested {
        const NAME: &'static str = "WithdrawalRequested";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}