// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct GlobalsSet {
        pub globals: Vec<u8>,
    }
    impl GlobalsSet {
        const TOPIC_ID: [u8; 32] = [
            218u8,
            54u8,
            242u8,
            98u8,
            82u8,
            170u8,
            53u8,
            210u8,
            98u8,
            56u8,
            52u8,
            94u8,
            44u8,
            51u8,
            156u8,
            219u8,
            254u8,
            178u8,
            108u8,
            228u8,
            151u8,
            33u8,
            3u8,
            12u8,
            168u8,
            233u8,
            7u8,
            171u8,
            23u8,
            1u8,
            172u8,
            143u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                globals: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "globals",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for GlobalsSet {
        const NAME: &'static str = "GlobalsSet";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct LenderAccepted {
        pub loan: Vec<u8>,
        pub pending_lender: Vec<u8>,
    }
    impl LenderAccepted {
        const TOPIC_ID: [u8; 32] = [
            90u8,
            140u8,
            47u8,
            157u8,
            164u8,
            162u8,
            221u8,
            223u8,
            8u8,
            56u8,
            243u8,
            16u8,
            129u8,
            189u8,
            75u8,
            112u8,
            233u8,
            56u8,
            40u8,
            133u8,
            223u8,
            57u8,
            218u8,
            29u8,
            249u8,
            247u8,
            31u8,
            199u8,
            170u8,
            215u8,
            47u8,
            184u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                loan: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "loan",
                )?,
                pending_lender: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "pending_lender",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for LenderAccepted {
        const NAME: &'static str = "LenderAccepted";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct LoanAddedToTransitionLoanManager {
        pub loan_manager: Vec<u8>,
        pub loan: Vec<u8>,
    }
    impl LoanAddedToTransitionLoanManager {
        const TOPIC_ID: [u8; 32] = [
            172u8,
            240u8,
            127u8,
            18u8,
            209u8,
            182u8,
            178u8,
            255u8,
            163u8,
            79u8,
            187u8,
            141u8,
            203u8,
            168u8,
            98u8,
            54u8,
            249u8,
            94u8,
            51u8,
            73u8,
            146u8,
            188u8,
            254u8,
            10u8,
            10u8,
            255u8,
            158u8,
            47u8,
            138u8,
            138u8,
            165u8,
            137u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                loan_manager: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "loan_manager",
                )?,
                loan: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "loan",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for LoanAddedToTransitionLoanManager {
        const NAME: &'static str = "LoanAddedToTransitionLoanManager";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct LoanManagerUpgraded {
        pub loan_manager: Vec<u8>,
        pub version: substreams::scalar::BigInt,
    }
    impl LoanManagerUpgraded {
        const TOPIC_ID: [u8; 32] = [
            76u8,
            188u8,
            48u8,
            3u8,
            51u8,
            87u8,
            113u8,
            211u8,
            52u8,
            206u8,
            35u8,
            107u8,
            88u8,
            163u8,
            164u8,
            127u8,
            170u8,
            78u8,
            40u8,
            181u8,
            28u8,
            246u8,
            108u8,
            94u8,
            117u8,
            63u8,
            163u8,
            102u8,
            99u8,
            216u8,
            67u8,
            29u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                loan_manager: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "loan_manager",
                )?,
                version: substreams_ethereum::abi::read_uint(log.data(), 0, "version")?,
            })
        }
    }
    impl substreams_ethereum::Event for LoanManagerUpgraded {
        const NAME: &'static str = "LoanManagerUpgraded";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OwnershipAccepted {
        pub new_owner: Vec<u8>,
    }
    impl OwnershipAccepted {
        const TOPIC_ID: [u8; 32] = [
            178u8,
            121u8,
            112u8,
            193u8,
            113u8,
            75u8,
            40u8,
            39u8,
            123u8,
            120u8,
            204u8,
            23u8,
            172u8,
            47u8,
            233u8,
            85u8,
            110u8,
            127u8,
            4u8,
            140u8,
            212u8,
            131u8,
            88u8,
            207u8,
            254u8,
            61u8,
            199u8,
            213u8,
            71u8,
            96u8,
            143u8,
            220u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                new_owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "new_owner",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OwnershipAccepted {
        const NAME: &'static str = "OwnershipAccepted";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct PendingAdminSet {
        pub pending_admin: Vec<u8>,
    }
    impl PendingAdminSet {
        const TOPIC_ID: [u8; 32] = [
            207u8,
            166u8,
            251u8,
            122u8,
            89u8,
            210u8,
            241u8,
            128u8,
            220u8,
            118u8,
            36u8,
            238u8,
            115u8,
            60u8,
            116u8,
            152u8,
            200u8,
            123u8,
            239u8,
            128u8,
            5u8,
            90u8,
            153u8,
            100u8,
            251u8,
            15u8,
            160u8,
            28u8,
            217u8,
            214u8,
            109u8,
            107u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                pending_admin: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pending_admin",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for PendingAdminSet {
        const NAME: &'static str = "PendingAdminSet";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct PendingLenderSet {
        pub loan: Vec<u8>,
        pub pending_lender: Vec<u8>,
    }
    impl PendingLenderSet {
        const TOPIC_ID: [u8; 32] = [
            145u8,
            212u8,
            30u8,
            140u8,
            203u8,
            153u8,
            49u8,
            224u8,
            125u8,
            2u8,
            72u8,
            107u8,
            124u8,
            81u8,
            170u8,
            21u8,
            147u8,
            184u8,
            53u8,
            39u8,
            64u8,
            4u8,
            186u8,
            41u8,
            0u8,
            16u8,
            99u8,
            115u8,
            22u8,
            219u8,
            220u8,
            114u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                loan: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "loan",
                )?,
                pending_lender: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "pending_lender",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for PendingLenderSet {
        const NAME: &'static str = "PendingLenderSet";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct RolledBackSetPendingLenders {
        pub loans: Vec<Vec<u8>>,
    }
    impl RolledBackSetPendingLenders {
        const TOPIC_ID: [u8; 32] = [
            240u8,
            49u8,
            166u8,
            228u8,
            44u8,
            211u8,
            108u8,
            31u8,
            50u8,
            62u8,
            227u8,
            122u8,
            91u8,
            94u8,
            159u8,
            213u8,
            86u8,
            244u8,
            217u8,
            160u8,
            165u8,
            74u8,
            228u8,
            58u8,
            112u8,
            133u8,
            35u8,
            125u8,
            58u8,
            231u8,
            230u8,
            117u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() < 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                loans: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        0,
                        "loans",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(
                            substreams_ethereum::abi::read_address(tail, at, "loans")?,
                        );
                        at += 32usize;
                    }
                    out
                },
            })
        }
    }
    impl substreams_ethereum::Event for RolledBackSetPendingLenders {
        const NAME: &'static str = "RolledBackSetPendingLenders";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct RolledBackTakeOwnershipOfLoans {
        pub loans: Vec<Vec<u8>>,
        pub debt_lockers: Vec<Vec<u8>>,
    }
    impl RolledBackTakeOwnershipOfLoans {
        const TOPIC_ID: [u8; 32] = [
            97u8,
            34u8,
            89u8,
            114u8,
            232u8,
            185u8,
            151u8,
            211u8,
            102u8,
            103u8,
            51u8,
            219u8,
            156u8,
            4u8,
            164u8,
            12u8,
            90u8,
            60u8,
            255u8,
            160u8,
            83u8,
            114u8,
            56u8,
            23u8,
            160u8,
            194u8,
            21u8,
            112u8,
            130u8,
            57u8,
            133u8,
            246u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() < 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                loans: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        0,
                        "loans",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(
                            substreams_ethereum::abi::read_address(tail, at, "loans")?,
                        );
                        at += 32usize;
                    }
                    out
                },
                debt_lockers: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        32,
                        "debt_lockers",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(
                            substreams_ethereum::abi::read_address(
                                tail,
                                at,
                                "debt_lockers",
                            )?,
                        );
                        at += 32usize;
                    }
                    out
                },
            })
        }
    }
    impl substreams_ethereum::Event for RolledBackTakeOwnershipOfLoans {
        const NAME: &'static str = "RolledBackTakeOwnershipOfLoans";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TokensAirdropped {
        pub pool_v1: Vec<u8>,
        pub pool_v2: Vec<u8>,
        pub lp1: Vec<u8>,
        pub lp2: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl TokensAirdropped {
        const TOPIC_ID: [u8; 32] = [
            47u8,
            196u8,
            84u8,
            158u8,
            29u8,
            195u8,
            9u8,
            46u8,
            207u8,
            193u8,
            118u8,
            73u8,
            47u8,
            136u8,
            236u8,
            251u8,
            255u8,
            128u8,
            136u8,
            32u8,
            205u8,
            115u8,
            212u8,
            91u8,
            227u8,
            36u8,
            116u8,
            184u8,
            197u8,
            76u8,
            41u8,
            108u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pool_v1: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool_v1",
                )?,
                pool_v2: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "pool_v2",
                )?,
                lp2: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "lp2",
                )?,
                lp1: substreams_ethereum::abi::read_address(log.data(), 0, "lp1")?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 32, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for TokensAirdropped {
        const NAME: &'static str = "TokensAirdropped";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}