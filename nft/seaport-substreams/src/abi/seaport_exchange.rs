// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct CounterIncremented {
        pub new_counter: substreams::scalar::BigInt,
        pub offerer: Vec<u8>,
    }
    impl CounterIncremented {
        const TOPIC_ID: [u8; 32] = [
            114u8,
            28u8,
            32u8,
            18u8,
            18u8,
            151u8,
            81u8,
            43u8,
            114u8,
            130u8,
            27u8,
            151u8,
            245u8,
            50u8,
            104u8,
            119u8,
            234u8,
            142u8,
            207u8,
            75u8,
            185u8,
            148u8,
            143u8,
            234u8,
            91u8,
            252u8,
            182u8,
            69u8,
            48u8,
            116u8,
            211u8,
            127u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                offerer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "offerer",
                )?,
                new_counter: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "new_counter",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for CounterIncremented {
        const NAME: &'static str = "CounterIncremented";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OrderCancelled {
        pub order_hash: [u8; 32usize],
        pub offerer: Vec<u8>,
        pub zone: Vec<u8>,
    }
    impl OrderCancelled {
        const TOPIC_ID: [u8; 32] = [
            107u8,
            172u8,
            192u8,
            29u8,
            190u8,
            68u8,
            36u8,
            150u8,
            6u8,
            143u8,
            125u8,
            35u8,
            78u8,
            221u8,
            129u8,
            31u8,
            26u8,
            95u8,
            131u8,
            50u8,
            67u8,
            224u8,
            174u8,
            200u8,
            36u8,
            248u8,
            106u8,
            184u8,
            97u8,
            243u8,
            201u8,
            13u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                offerer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "offerer",
                )?,
                zone: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "zone",
                )?,
                order_hash: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.data(), 0, "order_hash")?,
            })
        }
    }
    impl substreams_ethereum::Event for OrderCancelled {
        const NAME: &'static str = "OrderCancelled";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OrderFulfilled {
        pub order_hash: [u8; 32usize],
        pub offerer: Vec<u8>,
        pub zone: Vec<u8>,
        pub recipient: Vec<u8>,
        pub offer: Vec<
            (
                substreams::scalar::BigInt,
                Vec<u8>,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
        >,
        pub consideration: Vec<
            (
                substreams::scalar::BigInt,
                Vec<u8>,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                Vec<u8>,
            ),
        >,
    }
    impl OrderFulfilled {
        const TOPIC_ID: [u8; 32] = [
            157u8,
            154u8,
            248u8,
            227u8,
            141u8,
            102u8,
            198u8,
            46u8,
            44u8,
            18u8,
            240u8,
            34u8,
            82u8,
            73u8,
            253u8,
            157u8,
            114u8,
            28u8,
            84u8,
            184u8,
            63u8,
            72u8,
            217u8,
            53u8,
            44u8,
            151u8,
            198u8,
            202u8,
            205u8,
            203u8,
            111u8,
            49u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() < 192usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 192usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 192usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                offerer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "offerer",
                )?,
                zone: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "zone",
                )?,
                order_hash: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.data(), 0, "order_hash")?,
                recipient: substreams_ethereum::abi::read_address(
                    log.data(),
                    32,
                    "recipient",
                )?,
                offer: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        64,
                        "offer",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push({
                            let base = tail
                                .get(at..)
                                .ok_or_else(|| {
                                    format!(
                                        "unable to decode param '{}': need bytes at offset {}",
                                        "offer", at
                                    )
                                })?;
                            (
                                substreams_ethereum::abi::read_uint(base, 0, "offer")?,
                                substreams_ethereum::abi::read_address(base, 32, "offer")?,
                                substreams_ethereum::abi::read_uint(base, 64, "offer")?,
                                substreams_ethereum::abi::read_uint(base, 96, "offer")?,
                            )
                        });
                        at += 128usize;
                    }
                    out
                },
                consideration: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        log.data(),
                        96,
                        "consideration",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push({
                            let base = tail
                                .get(at..)
                                .ok_or_else(|| {
                                    format!(
                                        "unable to decode param '{}': need bytes at offset {}",
                                        "consideration", at
                                    )
                                })?;
                            (
                                substreams_ethereum::abi::read_uint(
                                    base,
                                    0,
                                    "consideration",
                                )?,
                                substreams_ethereum::abi::read_address(
                                    base,
                                    32,
                                    "consideration",
                                )?,
                                substreams_ethereum::abi::read_uint(
                                    base,
                                    64,
                                    "consideration",
                                )?,
                                substreams_ethereum::abi::read_uint(
                                    base,
                                    96,
                                    "consideration",
                                )?,
                                substreams_ethereum::abi::read_address(
                                    base,
                                    128,
                                    "consideration",
                                )?,
                            )
                        });
                        at += 160usize;
                    }
                    out
                },
            })
        }
    }
    impl substreams_ethereum::Event for OrderFulfilled {
        const NAME: &'static str = "OrderFulfilled";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OrderValidated {
        pub order_hash: [u8; 32usize],
        pub offerer: Vec<u8>,
        pub zone: Vec<u8>,
    }
    impl OrderValidated {
        const TOPIC_ID: [u8; 32] = [
            253u8,
            227u8,
            97u8,
            87u8,
            74u8,
            6u8,
            107u8,
            68u8,
            179u8,
            181u8,
            254u8,
            152u8,
            168u8,
            113u8,
            8u8,
            183u8,
            86u8,
            94u8,
            50u8,
            115u8,
            39u8,
            149u8,
            76u8,
            79u8,
            174u8,
            234u8,
            86u8,
            164u8,
            230u8,
            73u8,
            26u8,
            10u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                offerer: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "offerer",
                )?,
                zone: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "zone",
                )?,
                order_hash: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.data(), 0, "order_hash")?,
            })
        }
    }
    impl substreams_ethereum::Event for OrderValidated {
        const NAME: &'static str = "OrderValidated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}