specVersion: v0.1.0

package:
  name: amphor-substreams
  version: v0.1.0
  description: Indexes Amphor restaking vaults on Ethereum. Tracks deposits and withdrawals on the Amphor LRT vault.
  url: https://github.com/streamingfast/substreams-chain-modules/tree/main/staking/amphor-substreams

imports:
  database: https://github.com/streamingfast/substreams-sink-database-changes/releases/download/v4.0.0/substreams-sink-database-changes-v4.0.0.spkg

protobuf:
  files:
    - amphor.proto
  importPaths:
    - ./proto
  excludePaths:
    - sf/substreams
    - google

binaries:
  default:
    type: wasm/rust-v1
    file: ../../target/wasm32-unknown-unknown/release/amphor_substreams.wasm

network: mainnet

modules:
  - name: map_events
    kind: map
    initialBlock: 20126194
    inputs:
      - source: sf.ethereum.type.v2.Block
    output:
      type: proto:amphor.types.v1.Events

  - name: db_out
    kind: map
    initialBlock: 20126194
    inputs:
      - map: map_events
    output:
      type: proto:sf.substreams.sink.database.v1.DatabaseChanges
