// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AccrueRewards {
        pub user_reward_amount: substreams::scalar::BigInt,
        pub protocol_reward_amount: substreams::scalar::BigInt,
    }
    impl AccrueRewards {
        const TOPIC_ID: [u8; 32] = [
            145u8,
            81u8,
            73u8,
            161u8,
            103u8,
            10u8,
            129u8,
            23u8,
            122u8,
            83u8,
            214u8,
            247u8,
            62u8,
            230u8,
            249u8,
            17u8,
            171u8,
            236u8,
            158u8,
            141u8,
            19u8,
            208u8,
            202u8,
            2u8,
            169u8,
            58u8,
            40u8,
            252u8,
            192u8,
            213u8,
            68u8,
            88u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                user_reward_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "user_reward_amount",
                )?,
                protocol_reward_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "protocol_reward_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for AccrueRewards {
        const NAME: &'static str = "AccrueRewards";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Redeem {
        pub user: Vec<u8>,
        pub unlock_requested_at: substreams::scalar::BigInt,
        pub share_amount: substreams::scalar::BigInt,
        pub avax_amount: substreams::scalar::BigInt,
    }
    impl Redeem {
        const TOPIC_ID: [u8; 32] = [
            189u8,
            80u8,
            52u8,
            255u8,
            189u8,
            71u8,
            228u8,
            231u8,
            42u8,
            148u8,
            186u8,
            162u8,
            205u8,
            183u8,
            76u8,
            111u8,
            173u8,
            115u8,
            203u8,
            59u8,
            205u8,
            193u8,
            48u8,
            54u8,
            183u8,
            46u8,
            200u8,
            48u8,
            111u8,
            90u8,
            118u8,
            70u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                user: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                unlock_requested_at: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "unlock_requested_at",
                )?,
                share_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "share_amount",
                )?,
                avax_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "avax_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Redeem {
        const NAME: &'static str = "Redeem";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Submitted {
        pub user: Vec<u8>,
        pub avax_amount: substreams::scalar::BigInt,
        pub share_amount: substreams::scalar::BigInt,
    }
    impl Submitted {
        const TOPIC_ID: [u8; 32] = [
            187u8,
            0u8,
            112u8,
            137u8,
            65u8,
            53u8,
            208u8,
            46u8,
            223u8,
            165u8,
            80u8,
            176u8,
            77u8,
            126u8,
            94u8,
            20u8,
            26u8,
            168u8,
            9u8,
            11u8,
            70u8,
            229u8,
            117u8,
            151u8,
            173u8,
            69u8,
            191u8,
            237u8,
            214u8,
            85u8,
            68u8,
            152u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                user: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                avax_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "avax_amount",
                )?,
                share_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "share_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Submitted {
        const NAME: &'static str = "Submitted";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}