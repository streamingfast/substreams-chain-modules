// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct PoolCollectionAdded {
        pub pool_type: substreams::scalar::BigInt,
        pub pool_collection: Vec<u8>,
    }
    impl PoolCollectionAdded {
        const TOPIC_ID: [u8; 32] = [
            90u8,
            232u8,
            119u8,
            25u8,
            215u8,
            60u8,
            176u8,
            250u8,
            187u8,
            33u8,
            159u8,
            14u8,
            75u8,
            110u8,
            10u8,
            97u8,
            78u8,
            215u8,
            80u8,
            111u8,
            138u8,
            8u8,
            189u8,
            178u8,
            11u8,
            235u8,
            243u8,
            19u8,
            87u8,
            49u8,
            81u8,
            183u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                pool_type: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "pool_type",
                )?,
                pool_collection: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "pool_collection",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for PoolCollectionAdded {
        const NAME: &'static str = "PoolCollectionAdded";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct TokensTraded {
        pub context_id: [u8; 32usize],
        pub source_token: Vec<u8>,
        pub target_token: Vec<u8>,
        pub source_amount: substreams::scalar::BigInt,
        pub target_amount: substreams::scalar::BigInt,
        pub bnt_amount: substreams::scalar::BigInt,
        pub target_fee_amount: substreams::scalar::BigInt,
        pub bnt_fee_amount: substreams::scalar::BigInt,
        pub trader: Vec<u8>,
    }
    impl TokensTraded {
        const TOPIC_ID: [u8; 32] = [
            92u8,
            2u8,
            194u8,
            187u8,
            45u8,
            29u8,
            8u8,
            35u8,
            23u8,
            235u8,
            35u8,
            145u8,
            108u8,
            162u8,
            123u8,
            62u8,
            124u8,
            41u8,
            67u8,
            152u8,
            182u8,
            0u8,
            97u8,
            162u8,
            173u8,
            84u8,
            241u8,
            195u8,
            192u8,
            24u8,
            195u8,
            24u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 192usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 192usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 192usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                context_id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "context_id")?,
                source_token: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "source_token",
                )?,
                target_token: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "target_token",
                )?,
                source_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "source_amount",
                )?,
                target_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "target_amount",
                )?,
                bnt_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "bnt_amount",
                )?,
                target_fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "target_fee_amount",
                )?,
                bnt_fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    128,
                    "bnt_fee_amount",
                )?,
                trader: substreams_ethereum::abi::read_address(
                    log.data(),
                    160,
                    "trader",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for TokensTraded {
        const NAME: &'static str = "TokensTraded";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}