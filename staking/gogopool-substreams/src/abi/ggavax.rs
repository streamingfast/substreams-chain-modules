// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub caller: Vec<u8>,
        pub owner: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            220u8,
            188u8,
            28u8,
            5u8,
            36u8,
            15u8,
            49u8,
            255u8,
            58u8,
            208u8,
            103u8,
            239u8,
            30u8,
            227u8,
            92u8,
            228u8,
            153u8,
            119u8,
            98u8,
            117u8,
            46u8,
            58u8,
            9u8,
            82u8,
            132u8,
            117u8,
            69u8,
            68u8,
            244u8,
            199u8,
            9u8,
            215u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                owner: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 32, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct DepositedFromStaking {
        pub caller: Vec<u8>,
        pub base_amt: substreams::scalar::BigInt,
        pub rewards_amt: substreams::scalar::BigInt,
    }
    impl DepositedFromStaking {
        const TOPIC_ID: [u8; 32] = [
            196u8,
            39u8,
            210u8,
            214u8,
            89u8,
            215u8,
            219u8,
            161u8,
            143u8,
            59u8,
            125u8,
            194u8,
            48u8,
            165u8,
            179u8,
            238u8,
            175u8,
            87u8,
            117u8,
            189u8,
            218u8,
            150u8,
            181u8,
            1u8,
            142u8,
            252u8,
            238u8,
            210u8,
            65u8,
            224u8,
            110u8,
            14u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                base_amt: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "base_amt",
                )?,
                rewards_amt: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "rewards_amt",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for DepositedFromStaking {
        const NAME: &'static str = "DepositedFromStaking";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct NewRewardsCycle {
        pub cycle_end: substreams::scalar::BigInt,
        pub rewards_amt: substreams::scalar::BigInt,
    }
    impl NewRewardsCycle {
        const TOPIC_ID: [u8; 32] = [
            69u8,
            100u8,
            104u8,
            208u8,
            213u8,
            194u8,
            73u8,
            209u8,
            233u8,
            194u8,
            235u8,
            3u8,
            210u8,
            15u8,
            42u8,
            102u8,
            39u8,
            51u8,
            74u8,
            200u8,
            102u8,
            120u8,
            56u8,
            1u8,
            31u8,
            29u8,
            240u8,
            1u8,
            56u8,
            22u8,
            241u8,
            207u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                cycle_end: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "cycle_end",
                )?,
                rewards_amt: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "rewards_amt",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NewRewardsCycle {
        const NAME: &'static str = "NewRewardsCycle";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdraw {
        pub caller: Vec<u8>,
        pub receiver: Vec<u8>,
        pub owner: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Withdraw {
        const TOPIC_ID: [u8; 32] = [
            251u8,
            222u8,
            121u8,
            125u8,
            32u8,
            28u8,
            104u8,
            27u8,
            145u8,
            5u8,
            101u8,
            41u8,
            17u8,
            158u8,
            11u8,
            2u8,
            64u8,
            124u8,
            123u8,
            185u8,
            106u8,
            74u8,
            44u8,
            117u8,
            192u8,
            31u8,
            201u8,
            102u8,
            114u8,
            50u8,
            200u8,
            219u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                owner: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 32, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Withdraw {
        const NAME: &'static str = "Withdraw";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawnForStaking {
        pub caller: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
    }
    impl WithdrawnForStaking {
        const TOPIC_ID: [u8; 32] = [
            20u8,
            67u8,
            138u8,
            134u8,
            6u8,
            197u8,
            18u8,
            253u8,
            160u8,
            122u8,
            30u8,
            157u8,
            254u8,
            252u8,
            55u8,
            20u8,
            24u8,
            237u8,
            84u8,
            41u8,
            70u8,
            51u8,
            139u8,
            250u8,
            28u8,
            135u8,
            120u8,
            142u8,
            144u8,
            59u8,
            77u8,
            104u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawnForStaking {
        const NAME: &'static str = "WithdrawnForStaking";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}