// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct NewExperimentalVault {
        pub token: Vec<u8>,
        pub deployer: Vec<u8>,
        pub vault: Vec<u8>,
        pub api_version: String,
    }
    impl NewExperimentalVault {
        const TOPIC_ID: [u8; 32] = [
            87u8,
            169u8,
            205u8,
            194u8,
            160u8,
            94u8,
            5u8,
            246u8,
            110u8,
            118u8,
            118u8,
            155u8,
            219u8,
            232u8,
            142u8,
            33u8,
            236u8,
            69u8,
            217u8,
            238u8,
            15u8,
            151u8,
            212u8,
            203u8,
            96u8,
            57u8,
            93u8,
            76u8,
            117u8,
            220u8,
            188u8,
            218u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() < 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                deployer: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "deployer",
                )?,
                vault: substreams_ethereum::abi::read_address(log.data(), 0, "vault")?,
                api_version: substreams_ethereum::abi::read_string(
                    log.data(),
                    32,
                    "api_version",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NewExperimentalVault {
        const NAME: &'static str = "NewExperimentalVault";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct NewGovernance {
        pub governance: Vec<u8>,
    }
    impl NewGovernance {
        const TOPIC_ID: [u8; 32] = [
            79u8,
            56u8,
            105u8,
            117u8,
            234u8,
            28u8,
            47u8,
            124u8,
            241u8,
            132u8,
            91u8,
            8u8,
            190u8,
            224u8,
            6u8,
            38u8,
            251u8,
            182u8,
            36u8,
            236u8,
            173u8,
            22u8,
            37u8,
            77u8,
            99u8,
            217u8,
            187u8,
            155u8,
            168u8,
            101u8,
            38u8,
            222u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                governance: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "governance",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NewGovernance {
        const NAME: &'static str = "NewGovernance";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct NewRelease {
        pub release_id: substreams::scalar::BigInt,
        pub template: Vec<u8>,
        pub api_version: String,
    }
    impl NewRelease {
        const TOPIC_ID: [u8; 32] = [
            166u8,
            251u8,
            210u8,
            22u8,
            182u8,
            115u8,
            79u8,
            52u8,
            9u8,
            47u8,
            27u8,
            230u8,
            183u8,
            36u8,
            126u8,
            21u8,
            81u8,
            166u8,
            212u8,
            242u8,
            213u8,
            0u8,
            12u8,
            83u8,
            114u8,
            28u8,
            253u8,
            193u8,
            25u8,
            165u8,
            183u8,
            207u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() < 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                release_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "release_id",
                )?,
                template: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "template",
                )?,
                api_version: substreams_ethereum::abi::read_string(
                    log.data(),
                    32,
                    "api_version",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NewRelease {
        const NAME: &'static str = "NewRelease";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct NewVault {
        pub token: Vec<u8>,
        pub deployment_id: substreams::scalar::BigInt,
        pub vault: Vec<u8>,
        pub api_version: String,
    }
    impl NewVault {
        const TOPIC_ID: [u8; 32] = [
            206u8,
            8u8,
            153u8,
            5u8,
            186u8,
            74u8,
            77u8,
            98u8,
            37u8,
            83u8,
            188u8,
            181u8,
            100u8,
            111u8,
            210u8,
            62u8,
            137u8,
            92u8,
            37u8,
            111u8,
            3u8,
            118u8,
            238u8,
            224u8,
            78u8,
            153u8,
            230u8,
            28u8,
            236u8,
            29u8,
            199u8,
            232u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() < 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                deployment_id: substreams_ethereum::abi::read_uint(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "deployment_id",
                )?,
                vault: substreams_ethereum::abi::read_address(log.data(), 0, "vault")?,
                api_version: substreams_ethereum::abi::read_string(
                    log.data(),
                    32,
                    "api_version",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for NewVault {
        const NAME: &'static str = "NewVault";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct VaultTagged {
        pub vault: Vec<u8>,
        pub tag: String,
    }
    impl VaultTagged {
        const TOPIC_ID: [u8; 32] = [
            7u8,
            189u8,
            88u8,
            121u8,
            76u8,
            44u8,
            160u8,
            174u8,
            21u8,
            47u8,
            119u8,
            25u8,
            235u8,
            95u8,
            2u8,
            198u8,
            84u8,
            71u8,
            109u8,
            233u8,
            114u8,
            202u8,
            222u8,
            192u8,
            232u8,
            25u8,
            26u8,
            232u8,
            190u8,
            66u8,
            9u8,
            109u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() < 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                vault: substreams_ethereum::abi::read_address(log.data(), 0, "vault")?,
                tag: substreams_ethereum::abi::read_string(log.data(), 32, "tag")?,
            })
        }
    }
    impl substreams_ethereum::Event for VaultTagged {
        const NAME: &'static str = "VaultTagged";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}