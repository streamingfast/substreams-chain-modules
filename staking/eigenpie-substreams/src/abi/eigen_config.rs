// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AddedNewSupportedAsset {
        pub asset: Vec<u8>,
        pub receipt: Vec<u8>,
        pub deposit_limit: substreams::scalar::BigInt,
    }
    impl AddedNewSupportedAsset {
        const TOPIC_ID: [u8; 32] = [
            110u8,
            110u8,
            164u8,
            17u8,
            52u8,
            166u8,
            137u8,
            97u8,
            169u8,
            47u8,
            34u8,
            119u8,
            124u8,
            233u8,
            67u8,
            231u8,
            155u8,
            241u8,
            116u8,
            170u8,
            123u8,
            55u8,
            13u8,
            144u8,
            123u8,
            109u8,
            111u8,
            83u8,
            198u8,
            7u8,
            134u8,
            252u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                asset: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "asset",
                )?,
                receipt: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "receipt",
                )?,
                deposit_limit: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "deposit_limit",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for AddedNewSupportedAsset {
        const NAME: &'static str = "AddedNewSupportedAsset";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ReceiptTokenUpdated {
        pub asset: Vec<u8>,
        pub receipt: Vec<u8>,
    }
    impl ReceiptTokenUpdated {
        const TOPIC_ID: [u8; 32] = [
            200u8,
            248u8,
            238u8,
            41u8,
            135u8,
            255u8,
            163u8,
            101u8,
            123u8,
            228u8,
            110u8,
            177u8,
            137u8,
            243u8,
            98u8,
            172u8,
            249u8,
            208u8,
            182u8,
            20u8,
            210u8,
            98u8,
            111u8,
            192u8,
            91u8,
            118u8,
            203u8,
            174u8,
            62u8,
            255u8,
            5u8,
            248u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                asset: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "asset",
                )?,
                receipt: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "receipt",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ReceiptTokenUpdated {
        const NAME: &'static str = "ReceiptTokenUpdated";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}