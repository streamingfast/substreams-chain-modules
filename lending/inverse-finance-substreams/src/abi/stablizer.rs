// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Buy {
        pub user: Vec<u8>,
        pub purchased: substreams::scalar::BigInt,
        pub spent: substreams::scalar::BigInt,
    }
    impl Buy {
        const TOPIC_ID: [u8; 32] = [
            28u8,
            188u8,
            90u8,
            177u8,
            53u8,
            153u8,
            27u8,
            210u8,
            182u8,
            164u8,
            176u8,
            52u8,
            160u8,
            74u8,
            162u8,
            170u8,
            8u8,
            109u8,
            172u8,
            19u8,
            113u8,
            203u8,
            155u8,
            22u8,
            184u8,
            181u8,
            226u8,
            237u8,
            107u8,
            3u8,
            107u8,
            237u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                user: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                purchased: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "purchased",
                )?,
                spent: substreams_ethereum::abi::read_uint(log.data(), 32, "spent")?,
            })
        }
    }
    impl substreams_ethereum::Event for Buy {
        const NAME: &'static str = "Buy";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Sell {
        pub user: Vec<u8>,
        pub sold: substreams::scalar::BigInt,
        pub received: substreams::scalar::BigInt,
    }
    impl Sell {
        const TOPIC_ID: [u8; 32] = [
            237u8,
            122u8,
            20u8,
            79u8,
            173u8,
            20u8,
            128u8,
            77u8,
            92u8,
            36u8,
            145u8,
            69u8,
            227u8,
            224u8,
            226u8,
            182u8,
            58u8,
            158u8,
            180u8,
            85u8,
            183u8,
            106u8,
            238u8,
            91u8,
            201u8,
            45u8,
            113u8,
            30u8,
            155u8,
            186u8,
            62u8,
            74u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                user: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "user",
                )?,
                sold: substreams_ethereum::abi::read_uint(log.data(), 0, "sold")?,
                received: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "received",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Sell {
        const NAME: &'static str = "Sell";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}