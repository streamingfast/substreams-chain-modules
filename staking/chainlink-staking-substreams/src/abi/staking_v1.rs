// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Staked {
        pub staker: Vec<u8>,
        pub new_stake: substreams::scalar::BigInt,
        pub total_stake: substreams::scalar::BigInt,
    }
    impl Staked {
        const TOPIC_ID: [u8; 32] = [
            20u8,
            73u8,
            198u8,
            221u8,
            120u8,
            81u8,
            171u8,
            195u8,
            10u8,
            191u8,
            55u8,
            245u8,
            119u8,
            21u8,
            244u8,
            146u8,
            1u8,
            5u8,
            25u8,
            20u8,
            124u8,
            194u8,
            101u8,
            47u8,
            188u8,
            56u8,
            32u8,
            44u8,
            24u8,
            166u8,
            238u8,
            144u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                staker: substreams_ethereum::abi::read_address(log.data(), 0, "staker")?,
                new_stake: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "new_stake",
                )?,
                total_stake: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "total_stake",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Staked {
        const NAME: &'static str = "Staked";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Unstaked {
        pub staker: Vec<u8>,
        pub principal: substreams::scalar::BigInt,
        pub base_reward: substreams::scalar::BigInt,
        pub delegation_reward: substreams::scalar::BigInt,
    }
    impl Unstaked {
        const TOPIC_ID: [u8; 32] = [
            32u8,
            79u8,
            204u8,
            240u8,
            217u8,
            46u8,
            216u8,
            212u8,
            143u8,
            32u8,
            74u8,
            219u8,
            57u8,
            178u8,
            232u8,
            30u8,
            146u8,
            186u8,
            208u8,
            222u8,
            219u8,
            147u8,
            245u8,
            113u8,
            108u8,
            169u8,
            71u8,
            140u8,
            251u8,
            87u8,
            222u8,
            0u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                staker: substreams_ethereum::abi::read_address(log.data(), 0, "staker")?,
                principal: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "principal",
                )?,
                base_reward: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "base_reward",
                )?,
                delegation_reward: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "delegation_reward",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Unstaked {
        const NAME: &'static str = "Unstaked";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}