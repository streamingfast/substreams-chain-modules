// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub caller: Vec<u8>,
        pub receiver: Vec<u8>,
        pub should_compound: bool,
        pub deposited: substreams::scalar::BigInt,
        pub received_amount: substreams::scalar::BigInt,
        pub fee_amount: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            83u8,
            104u8,
            143u8,
            72u8,
            53u8,
            192u8,
            163u8,
            215u8,
            87u8,
            162u8,
            194u8,
            251u8,
            79u8,
            115u8,
            20u8,
            125u8,
            127u8,
            178u8,
            83u8,
            245u8,
            213u8,
            202u8,
            159u8,
            11u8,
            66u8,
            14u8,
            104u8,
            49u8,
            112u8,
            76u8,
            116u8,
            189u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                should_compound: substreams_ethereum::abi::read_bool(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "should_compound",
                )?,
                deposited: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "deposited",
                )?,
                received_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "received_amount",
                )?,
                fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "fee_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EmergencyWithdrawal {
        pub receiver: Vec<u8>,
        pub token: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl EmergencyWithdrawal {
        const TOPIC_ID: [u8; 32] = [
            148u8,
            149u8,
            208u8,
            49u8,
            144u8,
            167u8,
            154u8,
            67u8,
            229u8,
            52u8,
            201u8,
            227u8,
            40u8,
            255u8,
            50u8,
            47u8,
            98u8,
            131u8,
            38u8,
            19u8,
            131u8,
            245u8,
            241u8,
            156u8,
            128u8,
            149u8,
            100u8,
            246u8,
            173u8,
            90u8,
            87u8,
            179u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for EmergencyWithdrawal {
        const NAME: &'static str = "EmergencyWithdrawal";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct InitiateRedemption {
        pub assets: substreams::scalar::BigInt,
        pub post_fee_amount: substreams::scalar::BigInt,
        pub receiver: Vec<u8>,
    }
    impl InitiateRedemption {
        const TOPIC_ID: [u8; 32] = [
            161u8,
            38u8,
            252u8,
            109u8,
            119u8,
            119u8,
            225u8,
            16u8,
            246u8,
            252u8,
            83u8,
            241u8,
            31u8,
            52u8,
            180u8,
            105u8,
            90u8,
            247u8,
            53u8,
            36u8,
            242u8,
            178u8,
            245u8,
            88u8,
            93u8,
            200u8,
            27u8,
            78u8,
            20u8,
            135u8,
            51u8,
            242u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                post_fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "post_fee_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for InitiateRedemption {
        const NAME: &'static str = "InitiateRedemption";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct RedeemWithPxEth {
        pub assets: substreams::scalar::BigInt,
        pub post_fee_amount: substreams::scalar::BigInt,
        pub receiver: Vec<u8>,
    }
    impl RedeemWithPxEth {
        const TOPIC_ID: [u8; 32] = [
            239u8,
            101u8,
            215u8,
            40u8,
            73u8,
            209u8,
            204u8,
            166u8,
            75u8,
            0u8,
            64u8,
            160u8,
            25u8,
            7u8,
            177u8,
            243u8,
            48u8,
            248u8,
            106u8,
            157u8,
            62u8,
            1u8,
            133u8,
            108u8,
            230u8,
            189u8,
            85u8,
            205u8,
            184u8,
            161u8,
            110u8,
            12u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                post_fee_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "post_fee_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for RedeemWithPxEth {
        const NAME: &'static str = "RedeemWithPxEth";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ValidatorDeposit {
        pub pub_key: Vec<u8>,
    }
    impl ValidatorDeposit {
        const TOPIC_ID: [u8; 32] = [
            153u8,
            144u8,
            218u8,
            201u8,
            157u8,
            136u8,
            13u8,
            31u8,
            202u8,
            172u8,
            72u8,
            92u8,
            183u8,
            76u8,
            225u8,
            202u8,
            214u8,
            179u8,
            252u8,
            46u8,
            236u8,
            183u8,
            214u8,
            85u8,
            11u8,
            246u8,
            118u8,
            4u8,
            50u8,
            184u8,
            205u8,
            244u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() < 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                pub_key: substreams_ethereum::abi::read_bytes(log.data(), 0, "pub_key")?,
            })
        }
    }
    impl substreams_ethereum::Event for ValidatorDeposit {
        const NAME: &'static str = "ValidatorDeposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}