// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct ClaimedRedeemRequest {
        pub redeem_request_id: substreams::scalar::BigInt,
        pub recipient: Vec<u8>,
        pub eth_amount: substreams::scalar::BigInt,
        pub ls_eth_amount: substreams::scalar::BigInt,
        pub remaining_ls_eth_amount: substreams::scalar::BigInt,
    }
    impl ClaimedRedeemRequest {
        const TOPIC_ID: [u8; 32] = [
            37u8,
            244u8,
            223u8,
            165u8,
            240u8,
            112u8,
            61u8,
            76u8,
            80u8,
            155u8,
            215u8,
            33u8,
            110u8,
            112u8,
            248u8,
            55u8,
            143u8,
            65u8,
            148u8,
            51u8,
            193u8,
            72u8,
            64u8,
            193u8,
            79u8,
            62u8,
            170u8,
            219u8,
            96u8,
            100u8,
            42u8,
            209u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                redeem_request_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "redeem_request_id",
                )?,
                recipient: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "recipient",
                )?,
                eth_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "eth_amount",
                )?,
                ls_eth_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "ls_eth_amount",
                )?,
                remaining_ls_eth_amount: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "remaining_ls_eth_amount",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for ClaimedRedeemRequest {
        const NAME: &'static str = "ClaimedRedeemRequest";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct RequestedRedeem {
        pub owner: Vec<u8>,
        pub height: substreams::scalar::BigInt,
        pub amount: substreams::scalar::BigInt,
        pub max_redeemable_eth: substreams::scalar::BigInt,
        pub id: substreams::scalar::BigInt,
    }
    impl RequestedRedeem {
        const TOPIC_ID: [u8; 32] = [
            154u8,
            27u8,
            185u8,
            96u8,
            120u8,
            58u8,
            134u8,
            121u8,
            244u8,
            32u8,
            54u8,
            240u8,
            193u8,
            254u8,
            137u8,
            40u8,
            143u8,
            210u8,
            121u8,
            237u8,
            84u8,
            90u8,
            25u8,
            248u8,
            107u8,
            210u8,
            132u8,
            199u8,
            41u8,
            71u8,
            177u8,
            135u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                height: substreams_ethereum::abi::read_uint(log.data(), 0, "height")?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 32, "amount")?,
                max_redeemable_eth: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "max_redeemable_eth",
                )?,
                id: substreams_ethereum::abi::read_uint(log.data(), 96, "id")?,
            })
        }
    }
    impl substreams_ethereum::Event for RequestedRedeem {
        const NAME: &'static str = "RequestedRedeem";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}