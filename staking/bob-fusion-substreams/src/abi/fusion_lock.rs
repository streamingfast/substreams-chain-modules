// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub deposit_owner: Vec<u8>,
        pub token: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub deposit_time: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            220u8,
            188u8,
            28u8,
            5u8,
            36u8,
            15u8,
            49u8,
            255u8,
            58u8,
            208u8,
            103u8,
            239u8,
            30u8,
            227u8,
            92u8,
            228u8,
            153u8,
            119u8,
            98u8,
            117u8,
            46u8,
            58u8,
            9u8,
            82u8,
            132u8,
            117u8,
            69u8,
            68u8,
            244u8,
            199u8,
            9u8,
            215u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                deposit_owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "deposit_owner",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                deposit_time: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "deposit_time",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawToL1 {
        pub owner: Vec<u8>,
        pub token: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl WithdrawToL1 {
        const TOPIC_ID: [u8; 32] = [
            43u8,
            45u8,
            29u8,
            161u8,
            235u8,
            9u8,
            196u8,
            166u8,
            195u8,
            25u8,
            71u8,
            87u8,
            154u8,
            249u8,
            105u8,
            147u8,
            108u8,
            11u8,
            156u8,
            87u8,
            219u8,
            237u8,
            33u8,
            138u8,
            224u8,
            176u8,
            66u8,
            106u8,
            248u8,
            101u8,
            51u8,
            222u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawToL1 {
        const NAME: &'static str = "WithdrawToL1";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawToL2 {
        pub owner: Vec<u8>,
        pub receiver: Vec<u8>,
        pub l1_token: Vec<u8>,
        pub l2_token: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
    }
    impl WithdrawToL2 {
        const TOPIC_ID: [u8; 32] = [
            212u8,
            189u8,
            32u8,
            149u8,
            16u8,
            36u8,
            247u8,
            218u8,
            153u8,
            245u8,
            114u8,
            99u8,
            225u8,
            14u8,
            108u8,
            177u8,
            179u8,
            42u8,
            213u8,
            67u8,
            194u8,
            96u8,
            253u8,
            174u8,
            209u8,
            205u8,
            6u8,
            95u8,
            94u8,
            203u8,
            158u8,
            255u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "owner",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                l1_token: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "l1_token",
                )?,
                l2_token: substreams_ethereum::abi::read_address(
                    log.data(),
                    0,
                    "l2_token",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 32, "amount")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawToL2 {
        const NAME: &'static str = "WithdrawToL2";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}