// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AccrueInterest {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
    }
    impl AccrueInterest {
        const METHOD_ID: [u8; 4] = [21u8, 28u8, 26u8, 222u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 160usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 160usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for AccrueInterest {
        const NAME: &'static str = "accrueInterest";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Borrow {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
    }
    impl Borrow {
        const METHOD_ID: [u8; 4] = [80u8, 216u8, 205u8, 75u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                shares: substreams_ethereum::abi::read_uint(data, 192, "shares")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    224,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(data, 256, "receiver")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 288usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 288usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 192, &self.shares);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 224,
                &self.on_behalf,
            );
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 256,
                &self.receiver,
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(substreams::scalar::BigInt, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Borrow {
        const NAME: &'static str = "borrow";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (substreams::scalar::BigInt, substreams::scalar::BigInt),
    > for Borrow {
        fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct CreateMarket {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
    }
    impl CreateMarket {
        const METHOD_ID: [u8; 4] = [140u8, 19u8, 88u8, 162u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 160usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 160usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for CreateMarket {
        const NAME: &'static str = "createMarket";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct DomainSeparator {}
    impl DomainSeparator {
        const METHOD_ID: [u8; 4] = [54u8, 68u8, 229u8, 21u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Ok(Self {})
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 0usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 0usize);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<[u8; 32usize], String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<[u8; 32usize], String> {
            Ok(substreams_ethereum::abi::read_fixed_bytes::<32>(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<[u8; 32usize]> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for DomainSeparator {
        const NAME: &'static str = "DOMAIN_SEPARATOR";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<[u8; 32usize]> for DomainSeparator {
        fn output(data: &[u8]) -> Result<[u8; 32usize], String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EnableIrm {
        pub irm: Vec<u8>,
    }
    impl EnableIrm {
        const METHOD_ID: [u8; 4] = [90u8, 100u8, 245u8, 30u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                irm: substreams_ethereum::abi::read_address(data, 0, "irm")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_address_at(&mut out, base + 0, &self.irm);
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for EnableIrm {
        const NAME: &'static str = "enableIrm";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EnableLltv {
        pub lltv: substreams::scalar::BigInt,
    }
    impl EnableLltv {
        const METHOD_ID: [u8; 4] = [77u8, 152u8, 169u8, 59u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                lltv: substreams_ethereum::abi::read_uint(data, 0, "lltv")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 0, &self.lltv);
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for EnableLltv {
        const NAME: &'static str = "enableLltv";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct ExtSloads {
        pub slots: Vec<[u8; 32usize]>,
    }
    impl ExtSloads {
        const METHOD_ID: [u8; 4] = [119u8, 132u8, 198u8, 133u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                slots: {
                    let (tail, count) = substreams_ethereum::abi::read_array_tail(
                        data,
                        0,
                        "slots",
                    )?;
                    let mut out = Vec::with_capacity(count.min(1024));
                    let mut at = 0usize;
                    for _ in 0..count {
                        out.push(
                            substreams_ethereum::abi::read_fixed_bytes::<
                                32,
                            >(tail, at, "slots")?,
                        );
                        at += 32usize;
                    }
                    out
                },
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                {
                    let count = self.slots.len();
                    substreams_ethereum::abi::write_offset(&mut out, count);
                    let base_0 = substreams_ethereum::abi::reserve_head(
                        &mut out,
                        count * 32usize,
                    );
                    for (index, element_0) in self.slots.iter().enumerate() {
                        let slot = index * 32usize;
                        substreams_ethereum::abi::write_fixed_bytes_at(
                            &mut out,
                            base_0 + slot,
                            element_0.as_ref(),
                        );
                    }
                };
                substreams_ethereum::abi::backfill_offset(&mut out, base + 0, at);
            };
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Vec<[u8; 32usize]>, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<Vec<[u8; 32usize]>, String> {
            Ok({
                let (tail, count) = substreams_ethereum::abi::read_array_tail(
                    data,
                    0,
                    "output",
                )?;
                let mut out = Vec::with_capacity(count.min(1024));
                let mut at = 0usize;
                for _ in 0..count {
                    out.push(
                        substreams_ethereum::abi::read_fixed_bytes::<
                            32,
                        >(tail, at, "output")?,
                    );
                    at += 32usize;
                }
                out
            })
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<Vec<[u8; 32usize]>> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for ExtSloads {
        const NAME: &'static str = "extSloads";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<Vec<[u8; 32usize]>> for ExtSloads {
        fn output(data: &[u8]) -> Result<Vec<[u8; 32usize]>, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct FeeRecipient {}
    impl FeeRecipient {
        const METHOD_ID: [u8; 4] = [70u8, 144u8, 72u8, 64u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Ok(Self {})
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 0usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 0usize);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Vec<u8>, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<Vec<u8>, String> {
            Ok(substreams_ethereum::abi::read_address(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<Vec<u8>> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for FeeRecipient {
        const NAME: &'static str = "feeRecipient";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<Vec<u8>> for FeeRecipient {
        fn output(data: &[u8]) -> Result<Vec<u8>, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct FlashLoan {
        pub token: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub data: Vec<u8>,
    }
    impl FlashLoan {
        const METHOD_ID: [u8; 4] = [224u8, 35u8, 43u8, 66u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                token: substreams_ethereum::abi::read_address(data, 0, "token")?,
                assets: substreams_ethereum::abi::read_uint(data, 32, "assets")?,
                data: substreams_ethereum::abi::read_bytes(data, 64, "data")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 96usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 96usize);
            substreams_ethereum::abi::write_address_at(&mut out, base + 0, &self.token);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 32, &self.assets);
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                substreams_ethereum::abi::write_bytes_tail(&mut out, &self.data);
                substreams_ethereum::abi::backfill_offset(&mut out, base + 64, at);
            };
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for FlashLoan {
        const NAME: &'static str = "flashLoan";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IdToMarketParams {
        pub param0: [u8; 32usize],
    }
    impl IdToMarketParams {
        const METHOD_ID: [u8; 4] = [44u8, 60u8, 145u8, 87u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(data, 0, "param0")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_fixed_bytes_at(
                &mut out,
                base + 0,
                self.param0.as_ref(),
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<
            (Vec<u8>, Vec<u8>, Vec<u8>, Vec<u8>, substreams::scalar::BigInt),
            String,
        > {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<
            (Vec<u8>, Vec<u8>, Vec<u8>, Vec<u8>, substreams::scalar::BigInt),
            String,
        > {
            Ok((
                substreams_ethereum::abi::read_address(data, 0, "output")?,
                substreams_ethereum::abi::read_address(data, 32, "output")?,
                substreams_ethereum::abi::read_address(data, 64, "output")?,
                substreams_ethereum::abi::read_address(data, 96, "output")?,
                substreams_ethereum::abi::read_uint(data, 128, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(Vec<u8>, Vec<u8>, Vec<u8>, Vec<u8>, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for IdToMarketParams {
        const NAME: &'static str = "idToMarketParams";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (Vec<u8>, Vec<u8>, Vec<u8>, Vec<u8>, substreams::scalar::BigInt),
    > for IdToMarketParams {
        fn output(
            data: &[u8],
        ) -> Result<
            (Vec<u8>, Vec<u8>, Vec<u8>, Vec<u8>, substreams::scalar::BigInt),
            String,
        > {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IsAuthorized {
        pub param0: Vec<u8>,
        pub param1: Vec<u8>,
    }
    impl IsAuthorized {
        const METHOD_ID: [u8; 4] = [101u8, 228u8, 173u8, 158u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_address(data, 0, "param0")?,
                param1: substreams_ethereum::abi::read_address(data, 32, "param1")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 64usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 64usize);
            substreams_ethereum::abi::write_address_at(&mut out, base + 0, &self.param0);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 32,
                &self.param1,
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<bool, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<bool, String> {
            Ok(substreams_ethereum::abi::read_bool(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<bool> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for IsAuthorized {
        const NAME: &'static str = "isAuthorized";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<bool> for IsAuthorized {
        fn output(data: &[u8]) -> Result<bool, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IsIrmEnabled {
        pub param0: Vec<u8>,
    }
    impl IsIrmEnabled {
        const METHOD_ID: [u8; 4] = [242u8, 184u8, 99u8, 206u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_address(data, 0, "param0")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_address_at(&mut out, base + 0, &self.param0);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<bool, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<bool, String> {
            Ok(substreams_ethereum::abi::read_bool(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<bool> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for IsIrmEnabled {
        const NAME: &'static str = "isIrmEnabled";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<bool> for IsIrmEnabled {
        fn output(data: &[u8]) -> Result<bool, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IsLltvEnabled {
        pub param0: substreams::scalar::BigInt,
    }
    impl IsLltvEnabled {
        const METHOD_ID: [u8; 4] = [180u8, 133u8, 243u8, 184u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_uint(data, 0, "param0")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 0, &self.param0);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<bool, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<bool, String> {
            Ok(substreams_ethereum::abi::read_bool(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<bool> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for IsLltvEnabled {
        const NAME: &'static str = "isLltvEnabled";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<bool> for IsLltvEnabled {
        fn output(data: &[u8]) -> Result<bool, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Liquidate {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub borrower: Vec<u8>,
        pub seized_assets: substreams::scalar::BigInt,
        pub repaid_shares: substreams::scalar::BigInt,
        pub data: Vec<u8>,
    }
    impl Liquidate {
        const METHOD_ID: [u8; 4] = [216u8, 234u8, 188u8, 184u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                borrower: substreams_ethereum::abi::read_address(data, 160, "borrower")?,
                seized_assets: substreams_ethereum::abi::read_uint(
                    data,
                    192,
                    "seized_assets",
                )?,
                repaid_shares: substreams_ethereum::abi::read_uint(
                    data,
                    224,
                    "repaid_shares",
                )?,
                data: substreams_ethereum::abi::read_bytes(data, 256, "data")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 288usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 288usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 160,
                &self.borrower,
            );
            substreams_ethereum::abi::write_uint_at(
                &mut out,
                base + 192,
                &self.seized_assets,
            );
            substreams_ethereum::abi::write_uint_at(
                &mut out,
                base + 224,
                &self.repaid_shares,
            );
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                substreams_ethereum::abi::write_bytes_tail(&mut out, &self.data);
                substreams_ethereum::abi::backfill_offset(&mut out, base + 256, at);
            };
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(substreams::scalar::BigInt, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Liquidate {
        const NAME: &'static str = "liquidate";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (substreams::scalar::BigInt, substreams::scalar::BigInt),
    > for Liquidate {
        fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Market {
        pub param0: [u8; 32usize],
    }
    impl Market {
        const METHOD_ID: [u8; 4] = [92u8, 96u8, 227u8, 154u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(data, 0, "param0")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_fixed_bytes_at(
                &mut out,
                base + 0,
                self.param0.as_ref(),
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
                substreams_ethereum::abi::read_uint(data, 64, "output")?,
                substreams_ethereum::abi::read_uint(data, 96, "output")?,
                substreams_ethereum::abi::read_uint(data, 128, "output")?,
                substreams_ethereum::abi::read_uint(data, 160, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
        > {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Market {
        const NAME: &'static str = "market";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
        ),
    > for Market {
        fn output(
            data: &[u8],
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Nonce {
        pub param0: Vec<u8>,
    }
    impl Nonce {
        const METHOD_ID: [u8; 4] = [112u8, 174u8, 146u8, 210u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_address(data, 0, "param0")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_address_at(&mut out, base + 0, &self.param0);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<substreams::scalar::BigInt, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<substreams::scalar::BigInt, String> {
            Ok(substreams_ethereum::abi::read_uint(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<substreams::scalar::BigInt> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Nonce {
        const NAME: &'static str = "nonce";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<substreams::scalar::BigInt> for Nonce {
        fn output(data: &[u8]) -> Result<substreams::scalar::BigInt, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Owner {}
    impl Owner {
        const METHOD_ID: [u8; 4] = [141u8, 165u8, 203u8, 91u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Ok(Self {})
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 0usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 0usize);
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Vec<u8>, String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(data: &[u8]) -> Result<Vec<u8>, String> {
            Ok(substreams_ethereum::abi::read_address(data, 0, "output")?)
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(&self, address: Vec<u8>) -> Option<Vec<u8>> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Owner {
        const NAME: &'static str = "owner";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<Vec<u8>> for Owner {
        fn output(data: &[u8]) -> Result<Vec<u8>, String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Position {
        pub param0: [u8; 32usize],
        pub param1: Vec<u8>,
    }
    impl Position {
        const METHOD_ID: [u8; 4] = [147u8, 197u8, 32u8, 98u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                param0: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(data, 0, "param0")?,
                param1: substreams_ethereum::abi::read_address(data, 32, "param1")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 64usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 64usize);
            substreams_ethereum::abi::write_fixed_bytes_at(
                &mut out,
                base + 0,
                self.param0.as_ref(),
            );
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 32,
                &self.param1,
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
                substreams_ethereum::abi::read_uint(data, 64, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
        > {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Position {
        const NAME: &'static str = "position";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
        ),
    > for Position {
        fn output(
            data: &[u8],
        ) -> Result<
            (
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
                substreams::scalar::BigInt,
            ),
            String,
        > {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Repay {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub data: Vec<u8>,
    }
    impl Repay {
        const METHOD_ID: [u8; 4] = [32u8, 183u8, 110u8, 129u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                shares: substreams_ethereum::abi::read_uint(data, 192, "shares")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    224,
                    "on_behalf",
                )?,
                data: substreams_ethereum::abi::read_bytes(data, 256, "data")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 288usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 288usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 192, &self.shares);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 224,
                &self.on_behalf,
            );
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                substreams_ethereum::abi::write_bytes_tail(&mut out, &self.data);
                substreams_ethereum::abi::backfill_offset(&mut out, base + 256, at);
            };
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(substreams::scalar::BigInt, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Repay {
        const NAME: &'static str = "repay";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (substreams::scalar::BigInt, substreams::scalar::BigInt),
    > for Repay {
        fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetAuthorization {
        pub authorized: Vec<u8>,
        pub new_is_authorized: bool,
    }
    impl SetAuthorization {
        const METHOD_ID: [u8; 4] = [238u8, 206u8, 160u8, 0u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                authorized: substreams_ethereum::abi::read_address(
                    data,
                    0,
                    "authorized",
                )?,
                new_is_authorized: substreams_ethereum::abi::read_bool(
                    data,
                    32,
                    "new_is_authorized",
                )?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 64usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 64usize);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 0,
                &self.authorized,
            );
            substreams_ethereum::abi::write_bool_at(
                &mut out,
                base + 32,
                &self.new_is_authorized,
            );
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SetAuthorization {
        const NAME: &'static str = "setAuthorization";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetAuthorizationWithSig {
        pub authorization: (
            Vec<u8>,
            Vec<u8>,
            bool,
            substreams::scalar::BigInt,
            substreams::scalar::BigInt,
        ),
        pub signature: (substreams::scalar::BigInt, [u8; 32usize], [u8; 32usize]),
    }
    impl SetAuthorizationWithSig {
        const METHOD_ID: [u8; 4] = [128u8, 105u8, 33u8, 143u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                authorization: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "authorization", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "authorization",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "authorization",
                        )?,
                        substreams_ethereum::abi::read_bool(base, 64, "authorization")?,
                        substreams_ethereum::abi::read_uint(base, 96, "authorization")?,
                        substreams_ethereum::abi::read_uint(base, 128, "authorization")?,
                    )
                },
                signature: {
                    let base = data
                        .get(160..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "signature", 160
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_uint(base, 0, "signature")?,
                        substreams_ethereum::abi::read_fixed_bytes::<
                            32,
                        >(base, 32, "signature")?,
                        substreams_ethereum::abi::read_fixed_bytes::<
                            32,
                        >(base, 64, "signature")?,
                    )
                },
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 256usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 256usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.authorization.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.authorization.1,
                );
                substreams_ethereum::abi::write_bool_at(
                    &mut out,
                    base + 0 + 64,
                    &self.authorization.2,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 96,
                    &self.authorization.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.authorization.4,
                );
            };
            {
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 160 + 0,
                    &self.signature.0,
                );
                substreams_ethereum::abi::write_fixed_bytes_at(
                    &mut out,
                    base + 160 + 32,
                    self.signature.1.as_ref(),
                );
                substreams_ethereum::abi::write_fixed_bytes_at(
                    &mut out,
                    base + 160 + 64,
                    self.signature.2.as_ref(),
                );
            };
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SetAuthorizationWithSig {
        const NAME: &'static str = "setAuthorizationWithSig";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetFee {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub new_fee: substreams::scalar::BigInt,
    }
    impl SetFee {
        const METHOD_ID: [u8; 4] = [43u8, 79u8, 1u8, 60u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                new_fee: substreams_ethereum::abi::read_uint(data, 160, "new_fee")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 192usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 192usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.new_fee);
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SetFee {
        const NAME: &'static str = "setFee";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetFeeRecipient {
        pub new_fee_recipient: Vec<u8>,
    }
    impl SetFeeRecipient {
        const METHOD_ID: [u8; 4] = [231u8, 75u8, 152u8, 27u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                new_fee_recipient: substreams_ethereum::abi::read_address(
                    data,
                    0,
                    "new_fee_recipient",
                )?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 0,
                &self.new_fee_recipient,
            );
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SetFeeRecipient {
        const NAME: &'static str = "setFeeRecipient";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetOwner {
        pub new_owner: Vec<u8>,
    }
    impl SetOwner {
        const METHOD_ID: [u8; 4] = [19u8, 175u8, 64u8, 53u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                new_owner: substreams_ethereum::abi::read_address(data, 0, "new_owner")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 32usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 32usize);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 0,
                &self.new_owner,
            );
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SetOwner {
        const NAME: &'static str = "setOwner";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Supply {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub data: Vec<u8>,
    }
    impl Supply {
        const METHOD_ID: [u8; 4] = [169u8, 154u8, 173u8, 137u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                shares: substreams_ethereum::abi::read_uint(data, 192, "shares")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    224,
                    "on_behalf",
                )?,
                data: substreams_ethereum::abi::read_bytes(data, 256, "data")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 288usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 288usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 192, &self.shares);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 224,
                &self.on_behalf,
            );
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                substreams_ethereum::abi::write_bytes_tail(&mut out, &self.data);
                substreams_ethereum::abi::backfill_offset(&mut out, base + 256, at);
            };
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(substreams::scalar::BigInt, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Supply {
        const NAME: &'static str = "supply";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (substreams::scalar::BigInt, substreams::scalar::BigInt),
    > for Supply {
        fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SupplyCollateral {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub data: Vec<u8>,
    }
    impl SupplyCollateral {
        const METHOD_ID: [u8; 4] = [35u8, 141u8, 101u8, 121u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    192,
                    "on_behalf",
                )?,
                data: substreams_ethereum::abi::read_bytes(data, 224, "data")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 256usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 256usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 192,
                &self.on_behalf,
            );
            {
                let at = substreams_ethereum::abi::tail_offset(&out, base);
                substreams_ethereum::abi::write_bytes_tail(&mut out, &self.data);
                substreams_ethereum::abi::backfill_offset(&mut out, base + 224, at);
            };
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for SupplyCollateral {
        const NAME: &'static str = "supplyCollateral";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdraw {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
    }
    impl Withdraw {
        const METHOD_ID: [u8; 4] = [92u8, 43u8, 234u8, 73u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                shares: substreams_ethereum::abi::read_uint(data, 192, "shares")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    224,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(data, 256, "receiver")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 288usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 288usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_uint_at(&mut out, base + 192, &self.shares);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 224,
                &self.on_behalf,
            );
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 256,
                &self.receiver,
            );
            out
        }
        pub fn output_call(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(call.return_data.as_ref())
        }
        pub fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Ok((
                substreams_ethereum::abi::read_uint(data, 0, "output")?,
                substreams_ethereum::abi::read_uint(data, 32, "output")?,
            ))
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
        pub fn call(
            &self,
            address: Vec<u8>,
        ) -> Option<(substreams::scalar::BigInt, substreams::scalar::BigInt)> {
            use substreams_ethereum::pb::eth::rpc;
            let rpc_calls = rpc::RpcCalls {
                calls: vec![rpc::RpcCall { to_addr : address, data : self.encode(), }],
            };
            let responses = substreams_ethereum::rpc::eth_call(&rpc_calls).responses;
            let response = responses.get(0).expect("one response should have existed");
            if response.failed {
                return None;
            }
            match Self::output(response.raw.as_ref()) {
                Ok(data) => Some(data),
                Err(err) => {
                    use substreams_ethereum::Function;
                    substreams::log::info!(
                        "Call output for function `{}` failed to decode with error: {}",
                        Self::NAME, err
                    );
                    None
                }
            }
        }
    }
    impl substreams_ethereum::Function for Withdraw {
        const NAME: &'static str = "withdraw";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
    impl substreams_ethereum::rpc::RPCDecodable<
        (substreams::scalar::BigInt, substreams::scalar::BigInt),
    > for Withdraw {
        fn output(
            data: &[u8],
        ) -> Result<(substreams::scalar::BigInt, substreams::scalar::BigInt), String> {
            Self::output(data)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawCollateral {
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
        pub assets: substreams::scalar::BigInt,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
    }
    impl WithdrawCollateral {
        const METHOD_ID: [u8; 4] = [135u8, 32u8, 49u8, 109u8];
        pub fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            let maybe_data = call.input.get(4..);
            if maybe_data.is_none() {
                return Err("no data to decode".to_string());
            }
            let data = maybe_data.unwrap();
            Ok(Self {
                market_params: {
                    let base = data
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
                assets: substreams_ethereum::abi::read_uint(data, 160, "assets")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    data,
                    192,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(data, 224, "receiver")?,
            })
        }
        pub fn encode(&self) -> Vec<u8> {
            let mut out: Vec<u8> = Vec::with_capacity(4 + 256usize);
            out.extend(Self::METHOD_ID);
            let base = substreams_ethereum::abi::reserve_head(&mut out, 256usize);
            {
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 0,
                    &self.market_params.0,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 32,
                    &self.market_params.1,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 64,
                    &self.market_params.2,
                );
                substreams_ethereum::abi::write_address_at(
                    &mut out,
                    base + 0 + 96,
                    &self.market_params.3,
                );
                substreams_ethereum::abi::write_uint_at(
                    &mut out,
                    base + 0 + 128,
                    &self.market_params.4,
                );
            };
            substreams_ethereum::abi::write_uint_at(&mut out, base + 160, &self.assets);
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 192,
                &self.on_behalf,
            );
            substreams_ethereum::abi::write_address_at(
                &mut out,
                base + 224,
                &self.receiver,
            );
            out
        }
        pub fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            match call.input.get(0..4) {
                Some(signature) => Self::METHOD_ID == signature,
                None => false,
            }
        }
    }
    impl substreams_ethereum::Function for WithdrawCollateral {
        const NAME: &'static str = "withdrawCollateral";
        fn match_call(call: &substreams_ethereum::pb::eth::v2::Call) -> bool {
            Self::match_call(call)
        }
        fn decode(
            call: &substreams_ethereum::pb::eth::v2::Call,
        ) -> Result<Self, String> {
            Self::decode(call)
        }
        fn encode(&self) -> Vec<u8> {
            self.encode()
        }
    }
}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct AccrueInterest {
        pub id: [u8; 32usize],
        pub prev_borrow_rate: substreams::scalar::BigInt,
        pub interest: substreams::scalar::BigInt,
        pub fee_shares: substreams::scalar::BigInt,
    }
    impl AccrueInterest {
        const TOPIC_ID: [u8; 32] = [
            157u8,
            155u8,
            213u8,
            1u8,
            208u8,
            101u8,
            125u8,
            125u8,
            254u8,
            65u8,
            95u8,
            119u8,
            154u8,
            98u8,
            10u8,
            98u8,
            183u8,
            139u8,
            197u8,
            8u8,
            221u8,
            192u8,
            137u8,
            31u8,
            187u8,
            216u8,
            183u8,
            172u8,
            15u8,
            143u8,
            206u8,
            135u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                prev_borrow_rate: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "prev_borrow_rate",
                )?,
                interest: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "interest",
                )?,
                fee_shares: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "fee_shares",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for AccrueInterest {
        const NAME: &'static str = "AccrueInterest";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Borrow {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Borrow {
        const TOPIC_ID: [u8; 32] = [
            87u8,
            9u8,
            84u8,
            84u8,
            11u8,
            237u8,
            107u8,
            19u8,
            4u8,
            168u8,
            125u8,
            254u8,
            129u8,
            90u8,
            94u8,
            218u8,
            74u8,
            100u8,
            143u8,
            112u8,
            151u8,
            161u8,
            98u8,
            64u8,
            220u8,
            216u8,
            92u8,
            155u8,
            95u8,
            212u8,
            42u8,
            67u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                caller: substreams_ethereum::abi::read_address(log.data(), 0, "caller")?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 32, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 64, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Borrow {
        const NAME: &'static str = "Borrow";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct CreateMarket {
        pub id: [u8; 32usize],
        pub market_params: (
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            Vec<u8>,
            substreams::scalar::BigInt,
        ),
    }
    impl CreateMarket {
        const TOPIC_ID: [u8; 32] = [
            172u8,
            75u8,
            36u8,
            0u8,
            241u8,
            105u8,
            34u8,
            11u8,
            12u8,
            10u8,
            253u8,
            222u8,
            122u8,
            11u8,
            50u8,
            231u8,
            117u8,
            186u8,
            114u8,
            126u8,
            161u8,
            203u8,
            48u8,
            179u8,
            95u8,
            147u8,
            92u8,
            218u8,
            171u8,
            134u8,
            131u8,
            172u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 160usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 160usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 160usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                market_params: {
                    let base = log
                        .data()
                        .get(0..)
                        .ok_or_else(|| {
                            format!(
                                "unable to decode param '{}': need bytes at offset {}",
                                "market_params", 0
                            )
                        })?;
                    (
                        substreams_ethereum::abi::read_address(
                            base,
                            0,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            32,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            64,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_address(
                            base,
                            96,
                            "market_params",
                        )?,
                        substreams_ethereum::abi::read_uint(base, 128, "market_params")?,
                    )
                },
            })
        }
    }
    impl substreams_ethereum::Event for CreateMarket {
        const NAME: &'static str = "CreateMarket";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EnableIrm {
        pub irm: Vec<u8>,
    }
    impl EnableIrm {
        const TOPIC_ID: [u8; 32] = [
            89u8,
            14u8,
            4u8,
            205u8,
            235u8,
            236u8,
            203u8,
            164u8,
            15u8,
            86u8,
            97u8,
            134u8,
            185u8,
            116u8,
            106u8,
            210u8,
            149u8,
            164u8,
            205u8,
            53u8,
            142u8,
            164u8,
            254u8,
            250u8,
            174u8,
            166u8,
            206u8,
            121u8,
            99u8,
            13u8,
            150u8,
            192u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                irm: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "irm",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for EnableIrm {
        const NAME: &'static str = "EnableIrm";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct EnableLltv {
        pub lltv: substreams::scalar::BigInt,
    }
    impl EnableLltv {
        const TOPIC_ID: [u8; 32] = [
            41u8,
            123u8,
            128u8,
            231u8,
            168u8,
            150u8,
            250u8,
            212u8,
            112u8,
            198u8,
            48u8,
            246u8,
            87u8,
            80u8,
            114u8,
            214u8,
            9u8,
            189u8,
            233u8,
            151u8,
            38u8,
            15u8,
            243u8,
            219u8,
            133u8,
            25u8,
            57u8,
            64u8,
            94u8,
            194u8,
            145u8,
            57u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 1usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 1usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 1usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                lltv: substreams_ethereum::abi::read_uint(log.data(), 0, "lltv")?,
            })
        }
    }
    impl substreams_ethereum::Event for EnableLltv {
        const NAME: &'static str = "EnableLltv";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct FlashLoan {
        pub caller: Vec<u8>,
        pub token: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
    }
    impl FlashLoan {
        const TOPIC_ID: [u8; 32] = [
            199u8,
            111u8,
            27u8,
            79u8,
            228u8,
            57u8,
            106u8,
            192u8,
            122u8,
            159u8,
            165u8,
            90u8,
            65u8,
            93u8,
            76u8,
            164u8,
            48u8,
            231u8,
            38u8,
            81u8,
            211u8,
            125u8,
            52u8,
            1u8,
            243u8,
            190u8,
            215u8,
            203u8,
            19u8,
            252u8,
            79u8,
            18u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                token: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "token",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
            })
        }
    }
    impl substreams_ethereum::Event for FlashLoan {
        const NAME: &'static str = "FlashLoan";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IncrementNonce {
        pub caller: Vec<u8>,
        pub authorizer: Vec<u8>,
        pub used_nonce: substreams::scalar::BigInt,
    }
    impl IncrementNonce {
        const TOPIC_ID: [u8; 32] = [
            165u8,
            138u8,
            241u8,
            160u8,
            199u8,
            13u8,
            186u8,
            12u8,
            122u8,
            166u8,
            13u8,
            26u8,
            26u8,
            20u8,
            126u8,
            189u8,
            97u8,
            0u8,
            13u8,
            22u8,
            144u8,
            169u8,
            104u8,
            130u8,
            138u8,
            199u8,
            24u8,
            188u8,
            169u8,
            39u8,
            242u8,
            199u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                authorizer: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "authorizer",
                )?,
                used_nonce: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "used_nonce",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for IncrementNonce {
        const NAME: &'static str = "IncrementNonce";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Liquidate {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub borrower: Vec<u8>,
        pub repaid_assets: substreams::scalar::BigInt,
        pub repaid_shares: substreams::scalar::BigInt,
        pub seized_assets: substreams::scalar::BigInt,
        pub bad_debt_assets: substreams::scalar::BigInt,
        pub bad_debt_shares: substreams::scalar::BigInt,
    }
    impl Liquidate {
        const TOPIC_ID: [u8; 32] = [
            164u8,
            148u8,
            110u8,
            222u8,
            69u8,
            208u8,
            198u8,
            240u8,
            106u8,
            15u8,
            92u8,
            233u8,
            44u8,
            154u8,
            211u8,
            180u8,
            117u8,
            20u8,
            82u8,
            210u8,
            254u8,
            14u8,
            37u8,
            1u8,
            7u8,
            131u8,
            188u8,
            171u8,
            87u8,
            166u8,
            126u8,
            65u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 160usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 160usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 160usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                caller: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                borrower: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "borrower",
                )?,
                repaid_assets: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "repaid_assets",
                )?,
                repaid_shares: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "repaid_shares",
                )?,
                seized_assets: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "seized_assets",
                )?,
                bad_debt_assets: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "bad_debt_assets",
                )?,
                bad_debt_shares: substreams_ethereum::abi::read_uint(
                    log.data(),
                    128,
                    "bad_debt_shares",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Liquidate {
        const NAME: &'static str = "Liquidate";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Repay {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Repay {
        const TOPIC_ID: [u8; 32] = [
            82u8,
            172u8,
            176u8,
            92u8,
            235u8,
            189u8,
            60u8,
            211u8,
            151u8,
            21u8,
            70u8,
            159u8,
            34u8,
            175u8,
            191u8,
            90u8,
            23u8,
            73u8,
            98u8,
            149u8,
            239u8,
            59u8,
            201u8,
            187u8,
            89u8,
            68u8,
            5u8,
            108u8,
            99u8,
            204u8,
            170u8,
            9u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                caller: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 32, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Repay {
        const NAME: &'static str = "Repay";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetAuthorization {
        pub caller: Vec<u8>,
        pub authorizer: Vec<u8>,
        pub authorized: Vec<u8>,
        pub new_is_authorized: bool,
    }
    impl SetAuthorization {
        const TOPIC_ID: [u8; 32] = [
            213u8,
            233u8,
            105u8,
            240u8,
            30u8,
            254u8,
            146u8,
            29u8,
            63u8,
            118u8,
            107u8,
            222u8,
            186u8,
            210u8,
            95u8,
            10u8,
            5u8,
            227u8,
            242u8,
            55u8,
            49u8,
            31u8,
            86u8,
            72u8,
            43u8,
            241u8,
            50u8,
            208u8,
            50u8,
            99u8,
            9u8,
            192u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                caller: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                authorizer: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "authorizer",
                )?,
                authorized: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "authorized",
                )?,
                new_is_authorized: substreams_ethereum::abi::read_bool(
                    log.data(),
                    0,
                    "new_is_authorized",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for SetAuthorization {
        const NAME: &'static str = "SetAuthorization";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetFee {
        pub id: [u8; 32usize],
        pub new_fee: substreams::scalar::BigInt,
    }
    impl SetFee {
        const TOPIC_ID: [u8; 32] = [
            19u8,
            157u8,
            111u8,
            88u8,
            233u8,
            161u8,
            39u8,
            34u8,
            150u8,
            103u8,
            200u8,
            227u8,
            179u8,
            110u8,
            136u8,
            137u8,
            10u8,
            102u8,
            207u8,
            200u8,
            171u8,
            16u8,
            36u8,
            221u8,
            197u8,
            19u8,
            225u8,
            137u8,
            225u8,
            37u8,
            183u8,
            91u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                new_fee: substreams_ethereum::abi::read_uint(log.data(), 0, "new_fee")?,
            })
        }
    }
    impl substreams_ethereum::Event for SetFee {
        const NAME: &'static str = "SetFee";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetFeeRecipient {
        pub new_fee_recipient: Vec<u8>,
    }
    impl SetFeeRecipient {
        const TOPIC_ID: [u8; 32] = [
            46u8,
            151u8,
            159u8,
            128u8,
            254u8,
            77u8,
            67u8,
            5u8,
            92u8,
            88u8,
            76u8,
            244u8,
            168u8,
            70u8,
            124u8,
            85u8,
            135u8,
            94u8,
            163u8,
            103u8,
            40u8,
            252u8,
            55u8,
            23u8,
            108u8,
            5u8,
            172u8,
            215u8,
            132u8,
            235u8,
            122u8,
            115u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                new_fee_recipient: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "new_fee_recipient",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for SetFeeRecipient {
        const NAME: &'static str = "SetFeeRecipient";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SetOwner {
        pub new_owner: Vec<u8>,
    }
    impl SetOwner {
        const TOPIC_ID: [u8; 32] = [
            22u8,
            125u8,
            62u8,
            156u8,
            16u8,
            22u8,
            171u8,
            128u8,
            229u8,
            136u8,
            2u8,
            202u8,
            157u8,
            161u8,
            12u8,
            229u8,
            198u8,
            160u8,
            244u8,
            222u8,
            188u8,
            70u8,
            162u8,
            231u8,
            162u8,
            205u8,
            158u8,
            86u8,
            137u8,
            154u8,
            79u8,
            181u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 0usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            Ok(Self {
                new_owner: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "new_owner",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for SetOwner {
        const NAME: &'static str = "SetOwner";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Supply {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Supply {
        const TOPIC_ID: [u8; 32] = [
            237u8,
            248u8,
            135u8,
            4u8,
            51u8,
            200u8,
            56u8,
            35u8,
            235u8,
            7u8,
            29u8,
            61u8,
            241u8,
            202u8,
            168u8,
            208u8,
            8u8,
            241u8,
            47u8,
            100u8,
            64u8,
            145u8,
            140u8,
            32u8,
            215u8,
            90u8,
            54u8,
            2u8,
            205u8,
            163u8,
            15u8,
            224u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                caller: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 32, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Supply {
        const NAME: &'static str = "Supply";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct SupplyCollateral {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
    }
    impl SupplyCollateral {
        const TOPIC_ID: [u8; 32] = [
            163u8,
            185u8,
            71u8,
            42u8,
            19u8,
            153u8,
            225u8,
            126u8,
            18u8,
            63u8,
            60u8,
            46u8,
            101u8,
            134u8,
            194u8,
            62u8,
            80u8,
            65u8,
            132u8,
            213u8,
            4u8,
            222u8,
            89u8,
            205u8,
            170u8,
            43u8,
            55u8,
            94u8,
            136u8,
            12u8,
            97u8,
            132u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                caller: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "caller",
                )?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 0, "assets")?,
            })
        }
    }
    impl substreams_ethereum::Event for SupplyCollateral {
        const NAME: &'static str = "SupplyCollateral";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdraw {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
        pub shares: substreams::scalar::BigInt,
    }
    impl Withdraw {
        const TOPIC_ID: [u8; 32] = [
            165u8,
            111u8,
            192u8,
            173u8,
            87u8,
            2u8,
            236u8,
            5u8,
            206u8,
            99u8,
            102u8,
            98u8,
            33u8,
            247u8,
            150u8,
            251u8,
            98u8,
            67u8,
            124u8,
            50u8,
            219u8,
            26u8,
            161u8,
            170u8,
            7u8,
            95u8,
            198u8,
            72u8,
            76u8,
            245u8,
            143u8,
            191u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                caller: substreams_ethereum::abi::read_address(log.data(), 0, "caller")?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 32, "assets")?,
                shares: substreams_ethereum::abi::read_uint(log.data(), 64, "shares")?,
            })
        }
    }
    impl substreams_ethereum::Event for Withdraw {
        const NAME: &'static str = "Withdraw";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct WithdrawCollateral {
        pub id: [u8; 32usize],
        pub caller: Vec<u8>,
        pub on_behalf: Vec<u8>,
        pub receiver: Vec<u8>,
        pub assets: substreams::scalar::BigInt,
    }
    impl WithdrawCollateral {
        const TOPIC_ID: [u8; 32] = [
            232u8,
            14u8,
            189u8,
            124u8,
            201u8,
            34u8,
            61u8,
            115u8,
            130u8,
            170u8,
            178u8,
            224u8,
            209u8,
            214u8,
            21u8,
            92u8,
            101u8,
            101u8,
            31u8,
            131u8,
            213u8,
            60u8,
            139u8,
            155u8,
            6u8,
            144u8,
            29u8,
            22u8,
            126u8,
            50u8,
            17u8,
            66u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                id: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(log.topic(1usize).expect("bounds already checked"), 0, "id")?,
                on_behalf: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "on_behalf",
                )?,
                receiver: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "receiver",
                )?,
                caller: substreams_ethereum::abi::read_address(log.data(), 0, "caller")?,
                assets: substreams_ethereum::abi::read_uint(log.data(), 32, "assets")?,
            })
        }
    }
    impl substreams_ethereum::Event for WithdrawCollateral {
        const NAME: &'static str = "WithdrawCollateral";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}