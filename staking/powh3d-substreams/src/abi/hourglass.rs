// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Transfer {
        pub from: Vec<u8>,
        pub to: Vec<u8>,
        pub tokens: substreams::scalar::BigInt,
    }
    impl Transfer {
        const TOPIC_ID: [u8; 32] = [
            221u8,
            242u8,
            82u8,
            173u8,
            27u8,
            226u8,
            200u8,
            155u8,
            105u8,
            194u8,
            176u8,
            104u8,
            252u8,
            55u8,
            141u8,
            170u8,
            149u8,
            43u8,
            167u8,
            241u8,
            99u8,
            196u8,
            161u8,
            22u8,
            40u8,
            245u8,
            90u8,
            77u8,
            245u8,
            35u8,
            179u8,
            239u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                from: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "from",
                )?,
                to: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "to",
                )?,
                tokens: substreams_ethereum::abi::read_uint(log.data(), 0, "tokens")?,
            })
        }
    }
    impl substreams_ethereum::Event for Transfer {
        const NAME: &'static str = "Transfer";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OnReinvestment {
        pub customer_address: Vec<u8>,
        pub ethereum_reinvested: substreams::scalar::BigInt,
        pub tokens_minted: substreams::scalar::BigInt,
    }
    impl OnReinvestment {
        const TOPIC_ID: [u8; 32] = [
            190u8,
            51u8,
            159u8,
            193u8,
            75u8,
            4u8,
            28u8,
            43u8,
            14u8,
            15u8,
            61u8,
            210u8,
            205u8,
            50u8,
            93u8,
            12u8,
            54u8,
            104u8,
            183u8,
            131u8,
            120u8,
            0u8,
            30u8,
            83u8,
            22u8,
            14u8,
            171u8,
            54u8,
            21u8,
            50u8,
            100u8,
            88u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                customer_address: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "customer_address",
                )?,
                ethereum_reinvested: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "ethereum_reinvested",
                )?,
                tokens_minted: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "tokens_minted",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OnReinvestment {
        const NAME: &'static str = "onReinvestment";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OnTokenPurchase {
        pub customer_address: Vec<u8>,
        pub incoming_ethereum: substreams::scalar::BigInt,
        pub tokens_minted: substreams::scalar::BigInt,
        pub referred_by: Vec<u8>,
    }
    impl OnTokenPurchase {
        const TOPIC_ID: [u8; 32] = [
            2u8,
            44u8,
            13u8,
            153u8,
            46u8,
            77u8,
            135u8,
            58u8,
            55u8,
            72u8,
            67u8,
            109u8,
            150u8,
            13u8,
            81u8,
            64u8,
            193u8,
            249u8,
            114u8,
            28u8,
            247u8,
            63u8,
            124u8,
            165u8,
            236u8,
            103u8,
            157u8,
            61u8,
            159u8,
            79u8,
            226u8,
            213u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 3usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 3usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 3usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                customer_address: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "customer_address",
                )?,
                referred_by: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "referred_by",
                )?,
                incoming_ethereum: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "incoming_ethereum",
                )?,
                tokens_minted: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "tokens_minted",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OnTokenPurchase {
        const NAME: &'static str = "onTokenPurchase";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OnTokenSell {
        pub customer_address: Vec<u8>,
        pub tokens_burned: substreams::scalar::BigInt,
        pub ethereum_earned: substreams::scalar::BigInt,
    }
    impl OnTokenSell {
        const TOPIC_ID: [u8; 32] = [
            196u8,
            130u8,
            55u8,
            57u8,
            197u8,
            120u8,
            125u8,
            44u8,
            161u8,
            126u8,
            64u8,
            74u8,
            164u8,
            125u8,
            85u8,
            105u8,
            174u8,
            113u8,
            223u8,
            180u8,
            156u8,
            191u8,
            33u8,
            179u8,
            246u8,
            21u8,
            46u8,
            210u8,
            56u8,
            163u8,
            17u8,
            57u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 64usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 64usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 64usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                customer_address: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "customer_address",
                )?,
                tokens_burned: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "tokens_burned",
                )?,
                ethereum_earned: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "ethereum_earned",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OnTokenSell {
        const NAME: &'static str = "onTokenSell";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct OnWithdraw {
        pub customer_address: Vec<u8>,
        pub ethereum_withdrawn: substreams::scalar::BigInt,
    }
    impl OnWithdraw {
        const TOPIC_ID: [u8; 32] = [
            204u8,
            173u8,
            151u8,
            61u8,
            205u8,
            4u8,
            60u8,
            125u8,
            104u8,
            3u8,
            137u8,
            219u8,
            67u8,
            120u8,
            189u8,
            107u8,
            151u8,
            117u8,
            219u8,
            113u8,
            36u8,
            9u8,
            46u8,
            158u8,
            4u8,
            34u8,
            201u8,
            228u8,
            109u8,
            121u8,
            133u8,
            220u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 32usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 32usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 32usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                customer_address: substreams_ethereum::abi::read_address(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "customer_address",
                )?,
                ethereum_withdrawn: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "ethereum_withdrawn",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for OnWithdraw {
        const NAME: &'static str = "onWithdraw";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}