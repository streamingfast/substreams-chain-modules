// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct DecreaseLiquidity {
        pub token_id: substreams::scalar::BigInt,
        pub liquidity: substreams::scalar::BigInt,
        pub amount0: substreams::scalar::BigInt,
        pub amount1: substreams::scalar::BigInt,
        pub tick_lower: substreams::scalar::BigInt,
        pub tick_upper: substreams::scalar::BigInt,
        pub pool: Vec<u8>,
    }
    impl DecreaseLiquidity {
        const TOPIC_ID: [u8; 32] = [
            235u8,
            125u8,
            36u8,
            219u8,
            206u8,
            83u8,
            112u8,
            31u8,
            191u8,
            39u8,
            116u8,
            24u8,
            222u8,
            147u8,
            13u8,
            57u8,
            7u8,
            240u8,
            111u8,
            160u8,
            96u8,
            19u8,
            57u8,
            92u8,
            247u8,
            190u8,
            164u8,
            180u8,
            81u8,
            186u8,
            106u8,
            192u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 192usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 192usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 192usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token_id",
                )?,
                liquidity: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "liquidity",
                )?,
                amount0: substreams_ethereum::abi::read_uint(log.data(), 32, "amount0")?,
                amount1: substreams_ethereum::abi::read_uint(log.data(), 64, "amount1")?,
                tick_lower: substreams_ethereum::abi::read_int(
                    log.data(),
                    96,
                    "tick_lower",
                )?,
                tick_upper: substreams_ethereum::abi::read_int(
                    log.data(),
                    128,
                    "tick_upper",
                )?,
                pool: substreams_ethereum::abi::read_address(log.data(), 160, "pool")?,
            })
        }
    }
    impl substreams_ethereum::Event for DecreaseLiquidity {
        const NAME: &'static str = "DecreaseLiquidity";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct IncreaseLiquidity {
        pub token_id: substreams::scalar::BigInt,
        pub liquidity: substreams::scalar::BigInt,
        pub amount0: substreams::scalar::BigInt,
        pub amount1: substreams::scalar::BigInt,
        pub tick_lower: substreams::scalar::BigInt,
        pub tick_upper: substreams::scalar::BigInt,
        pub pool: Vec<u8>,
    }
    impl IncreaseLiquidity {
        const TOPIC_ID: [u8; 32] = [
            188u8,
            2u8,
            127u8,
            16u8,
            112u8,
            39u8,
            113u8,
            238u8,
            250u8,
            17u8,
            63u8,
            63u8,
            233u8,
            141u8,
            108u8,
            120u8,
            111u8,
            66u8,
            157u8,
            106u8,
            51u8,
            153u8,
            63u8,
            241u8,
            249u8,
            219u8,
            4u8,
            75u8,
            20u8,
            123u8,
            123u8,
            12u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 2usize {
                return false;
            }
            if log.data().len() != 192usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 2usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 2usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 192usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 192usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                token_id: substreams_ethereum::abi::read_uint(
                    log.topic(1usize).expect("bounds already checked"),
                    0,
                    "token_id",
                )?,
                liquidity: substreams_ethereum::abi::read_uint(
                    log.data(),
                    0,
                    "liquidity",
                )?,
                amount0: substreams_ethereum::abi::read_uint(log.data(), 32, "amount0")?,
                amount1: substreams_ethereum::abi::read_uint(log.data(), 64, "amount1")?,
                tick_lower: substreams_ethereum::abi::read_int(
                    log.data(),
                    96,
                    "tick_lower",
                )?,
                tick_upper: substreams_ethereum::abi::read_int(
                    log.data(),
                    128,
                    "tick_upper",
                )?,
                pool: substreams_ethereum::abi::read_address(log.data(), 160, "pool")?,
            })
        }
    }
    impl substreams_ethereum::Event for IncreaseLiquidity {
        const NAME: &'static str = "IncreaseLiquidity";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}