// @generated
// This file was @generated by `substreams-ethereum-abigen`. Do not edit it by hand.

/// Contract's functions.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod functions {}
/// Contract's events.
#[allow(dead_code, unused_imports, unused_variables)]
pub mod events {
    #[derive(Debug, Clone, PartialEq)]
    pub struct Deposit {
        pub currency_code: substreams_ethereum::IndexedDynamicValue<String>,
        pub sender: Vec<u8>,
        pub payee: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub amount_usd: substreams::scalar::BigInt,
        pub rft_minted: substreams::scalar::BigInt,
    }
    impl Deposit {
        const TOPIC_ID: [u8; 32] = [
            183u8,
            138u8,
            248u8,
            145u8,
            176u8,
            232u8,
            194u8,
            230u8,
            23u8,
            194u8,
            169u8,
            20u8,
            113u8,
            196u8,
            10u8,
            204u8,
            178u8,
            85u8,
            158u8,
            42u8,
            165u8,
            51u8,
            180u8,
            23u8,
            70u8,
            169u8,
            210u8,
            229u8,
            128u8,
            180u8,
            188u8,
            51u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 96usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 96usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 96usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                currency_code: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(
                        log.topic(1usize).expect("bounds already checked"),
                        0,
                        "currency_code",
                    )?
                    .to_vec()
                    .into(),
                sender: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "sender",
                )?,
                payee: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "payee",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                amount_usd: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "amount_usd",
                )?,
                rft_minted: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "rft_minted",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Deposit {
        const NAME: &'static str = "Deposit";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
    #[derive(Debug, Clone, PartialEq)]
    pub struct Withdrawal {
        pub currency_code: substreams_ethereum::IndexedDynamicValue<String>,
        pub sender: Vec<u8>,
        pub payee: Vec<u8>,
        pub amount: substreams::scalar::BigInt,
        pub amount_usd: substreams::scalar::BigInt,
        pub rft_burned: substreams::scalar::BigInt,
        pub withdrawal_fee_rate: substreams::scalar::BigInt,
    }
    impl Withdrawal {
        const TOPIC_ID: [u8; 32] = [
            70u8,
            110u8,
            182u8,
            109u8,
            170u8,
            144u8,
            254u8,
            13u8,
            48u8,
            206u8,
            64u8,
            150u8,
            168u8,
            156u8,
            23u8,
            180u8,
            229u8,
            183u8,
            47u8,
            151u8,
            11u8,
            229u8,
            63u8,
            236u8,
            126u8,
            34u8,
            108u8,
            112u8,
            206u8,
            107u8,
            152u8,
            92u8,
        ];
        pub fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            if log.topic_count() != 4usize {
                return false;
            }
            if log.data().len() != 128usize {
                return false;
            }
            return log.topic(0).expect("bounds already checked") == Self::TOPIC_ID;
        }
        pub fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            if log.topic_count() != 4usize {
                return Err(
                    format!(
                        "unexpected topic count, expected {}, got {}", 4usize, log
                        .topic_count()
                    ),
                );
            }
            if log.data().len() < 128usize {
                return Err(
                    format!(
                        "data too short, expected at least {}, got {}", 128usize, log
                        .data().len()
                    ),
                );
            }
            Ok(Self {
                currency_code: substreams_ethereum::abi::read_fixed_bytes::<
                    32,
                >(
                        log.topic(1usize).expect("bounds already checked"),
                        0,
                        "currency_code",
                    )?
                    .to_vec()
                    .into(),
                sender: substreams_ethereum::abi::read_address(
                    log.topic(2usize).expect("bounds already checked"),
                    0,
                    "sender",
                )?,
                payee: substreams_ethereum::abi::read_address(
                    log.topic(3usize).expect("bounds already checked"),
                    0,
                    "payee",
                )?,
                amount: substreams_ethereum::abi::read_uint(log.data(), 0, "amount")?,
                amount_usd: substreams_ethereum::abi::read_uint(
                    log.data(),
                    32,
                    "amount_usd",
                )?,
                rft_burned: substreams_ethereum::abi::read_uint(
                    log.data(),
                    64,
                    "rft_burned",
                )?,
                withdrawal_fee_rate: substreams_ethereum::abi::read_uint(
                    log.data(),
                    96,
                    "withdrawal_fee_rate",
                )?,
            })
        }
    }
    impl substreams_ethereum::Event for Withdrawal {
        const NAME: &'static str = "Withdrawal";
        fn match_log<L: substreams_ethereum::LogLike>(log: &L) -> bool {
            Self::match_log(log)
        }
        fn decode<L: substreams_ethereum::LogLike>(log: &L) -> Result<Self, String> {
            Self::decode(log)
        }
    }
}